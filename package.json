{
  "private": true,
  "scripts": {
    "test:ui": "vitest run --config packages/ui/vitest.config.ts",
    "test:shopify": "vitest run --config packages/shopify/vitest.config.ts",
    "test:stats": "vitest run --config packages/stats/vitest.config.ts",
    "test:upholstery": "vitest run --config packages/upholstery/vitest.config.ts",
    "typecheck": "npm run typecheck --workspace managerbeyo-app-managers --workspace managerbeyo-app-workers --workspace managerbeyo-app-sellers && tsc -p packages/ui/tsconfig.json --noEmit && tsc -p packages/hooks/tsconfig.json --noEmit && tsc -p packages/realtime/tsconfig.json --noEmit && tsc -p packages/notifications/tsconfig.json --noEmit && tsc -p packages/celebration/tsconfig.json --noEmit && tsc -p packages/shopify/tsconfig.json --noEmit && tsc -p packages/stats/tsconfig.json --noEmit && tsc -p packages/upholstery/tsconfig.json --noEmit"
  },
  "workspaces": [
    "apps/managers-app/*",
    "apps/workers-app/*",
    "apps/selleres-app/*",
    "packages/*"
  ],
  "dependencies": {
    "socket.io-client": "^4.8.3",
    "ulid": "^3.0.2"
  }
}
