{
  "private": true,
  "scripts": {
    "test:ui": "vitest run --config packages/ui/vitest.config.ts",
    "test:shopify": "vitest run --config packages/shopify/vitest.config.ts",
    "test:presentation-builder": "vitest run --config packages/presentation-builder/vitest.config.ts",
    "test:presentation-runtime": "vitest run --config packages/presentation-runtime/vitest.config.ts",
    "test:stats": "vitest run --config packages/stats/vitest.config.ts",
    "test:upholstery": "vitest run --config packages/upholstery/vitest.config.ts",
    "test:pause-reasons": "vitest run --config packages/pause-reasons/vitest.config.ts",
    "test:workers-pause-reasons": "vitest run --config apps/workers-app/ManagerBeyo-app-workers/vitest.config.ts",
    "typecheck": "npm run typecheck --workspace managerbeyo-app-managers --workspace managerbeyo-app-workers --workspace managerbeyo-app-sellers --workspace managerbeyo-app-presentation-studio && tsc -p packages/ui/tsconfig.json --noEmit && tsc -p packages/hooks/tsconfig.json --noEmit && tsc -p packages/realtime/tsconfig.json --noEmit && tsc -p packages/notifications/tsconfig.json --noEmit && tsc -p packages/celebration/tsconfig.json --noEmit && tsc -p packages/shopify/tsconfig.json --noEmit && tsc -p packages/stats/tsconfig.json --noEmit && tsc -p packages/upholstery/tsconfig.json --noEmit && tsc -p packages/presentation-runtime/tsconfig.json --noEmit && tsc -p packages/presentation-builder/tsconfig.json --noEmit"
  },
  "workspaces": [
    "apps/managers-app/*",
    "apps/presentation-studio/*",
    "apps/workers-app/*",
    "apps/selleres-app/*",
    "packages/*"
  ],
  "dependencies": {
    "socket.io-client": "^4.8.3",
    "ulid": "^3.0.2"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7",
    "msw": "^2.15.0"
  }
}
