# HANDOFF_TO_BACKEND_<slug>_<YYYYMMDD>

## Metadata

- Handoff ID: `HANDOFF_TO_BACKEND_<slug>_<YYYYMMDD>`
- Created at (UTC): `<YYYY-MM-DDTHH:MM:SSZ>`
- Owner agent: `<agent_name>`
- Source frontend plan: `docs/architecture/under_construction/PLAN_<slug>_<YYYYMMDD>.md`

## Request to backend

- Required backend behavior:
- User-facing impact:
- Desired timeline:

## Frontend context

- Why the frontend needs this:
- Blocked frontend plan (if any): `docs/architecture/under_construction/PLAN_<slug>_<YYYYMMDD>.md`
- Clarifications required:
  - [ ] `<question>` — why this blocks frontend implementation

## Expected backend deliverables

1. `<endpoint / event / schema change>`
2. `<acceptance criterion>`

## Interface expectations

- Endpoint(s):
- Request shape:
- Response shape:
- Error cases:
- Socket events (if applicable):

## Frontend contract implications

- Architecture contracts affected:
  - `architecture/<file.md>`: `<reason>`
- Local extension updates needed:
  - `architecture/<file_local.md>`
