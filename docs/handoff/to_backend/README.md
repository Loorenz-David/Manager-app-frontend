# Handoff To Backend

Place frontend requests and plans that require backend execution — new endpoints, schema changes, socket event contract updates, or data model additions needed to support frontend feature delivery.
