# PLAN_clock_kiosk_master_20260729

## Metadata

- Plan ID: `PLAN_clock_kiosk_master_20260729`
- Status: `completed` (all executable child phases archived; Phase 7 fidelity/a11y review recorded 2026-07-30)
- Owner agent: `Claude (Fable)` — plan creator, orchestrator, UI designer
- Created at (UTC): `2026-07-29T13:30:00Z`
- Last updated at (UTC): `2026-07-30T10:15:41Z`
- Related issue/ticket: none provided
- Backend handoff (authoritative API contract): `docs/handoff/from_backend/HANDOFF_TO_FRONTEND_worker_shift_floor_app_20260729.md` — **BUILD-AHEAD CONTRACT**: most endpoints are not live yet; the frontend builds against the documented shapes with mocks and flips to real endpoints as backend phases land.
- Design handoff (authoritative look & interaction): `docs/architecture/under_construction/implementation/clock_in_out_app/image_design/design_readme.md` + 5 screenshots in the same folder.
- Backend gap list (what the design needs that the API does not yet provide): `BACKEND_REQUIREMENTS_clock_kiosk_20260729.md` (same folder).
- Plan type: **Master plan** — defines the phased sequence and shared decisions for the clock-in/out floor kiosk capability. It is not itself executable. Each phase has a child plan in `plans/`, an execution prompt in `prompts/execution/` (for Codex), and a review prompt in `prompts/review/` (for Opus). Child plans defer to this master wherever the two could disagree; corrections flow back into this master's Review log.

## Goal and intent

- Goal: build the fourth application of the monorepo — an always-on **shop-floor kiosk** running on a shared device (wall tablet / terminal / floor PC) where workers clock in/out of their shift with a personal **4-digit code** (or their working email). Declared states (lunch, cleaning, meeting…) are wrapped at the domain layer for future pages but have **no v1 UI** (decision #10). Composed of:
  1. `@beyo/worker-shifts` (`packages/worker-shifts`) — the shift **domain package**: types, query keys, API functions, query hooks, action hooks, roster matching. No UI. Reusable tomorrow by the workers/managers apps.
  2. `@beyo/clock-kiosk` (`packages/clock-kiosk`) — the kiosk **feature package**: the whole kiosk experience (keypad → identity confirm → action → result), its flow state machine, all screens/components, surface exports. The kiosk must be mountable as a page in any host app.
  3. `apps/floor-app/ManagerBeyo-app-floor` — a **thin shell** app: floor-scope auth, device configuration, kiosk chrome, providers, styling registration. Owns zero kiosk logic.
  4. Floor-scope **device auth** support in `@beyo/api-client` + `@beyo/auth` (non-expiring persisted token, no refresh).
- Business/user intent: workers walk up, type a code, see themselves confirmed (photo + name), tap one button, and walk away — one hand, gloves on, a few seconds per person. Managers get truthful shift machinery feeding the existing worker-stats surfaces.
- Non-goals (v1):
  - **Declared-states UI** (user decision 2026-07-29): the kiosk ships clock in/out only. The domain wrappers exist in `@beyo/worker-shifts` (Phase 1) because the package serves future pages, but no kiosk screen consumes them — Phase 5 is shelved.
  - Backend changes of any kind (gaps are documented in `BACKEND_REQUIREMENTS_clock_kiosk_20260729.md`, not implemented).
  - Managing `clock_in_code` assignment (backend phase 6; a managers-app feature later — not this capability).
  - The legacy `POST /worker-shifts/clock` toggle route (explicit routes only, per handoff §5).
  - Push notifications, Connecteam anything, payroll exports.
  - Editing the design images/readme or the backend handoff.

## Decisions (defaults chosen by the planner — flag disagreement before Phase 1 runs)

| # | Question | Decision |
|---|---|---|
| 1 | Naming | App `apps/floor-app/ManagerBeyo-app-floor`, package name `managerbeyo-app-floor`, dev port **5175**. Packages `@beyo/worker-shifts`, `@beyo/clock-kiosk`. |
| 2 | Shell shape + the **`rise` surface** (amended by user 2026-07-29) | **No bottom tabs, no TabSlideStack, no SlideStack.** The app copies the presentation-studio minimal `src/app/` (providers, router, RootRoute, surface-registry) and keeps the surface registry + `SurfaceProvider` engine — but this app does **not** use the existing surface types. A new surface type is added to `@beyo/ui`: **`rise`** — enters with a fade-in while sliding up, exits with a fade-out while sliding down (`RiseSurface` shell, registered additively in the SurfaceProvider renderer; existing types untouched). The rise surface is used **everywhere** in the kiosk: the flow screens themselves (identity confirm, result/summary) open as rise surfaces over the **always-mounted keypad page**, and overlay surfaces (device settings) use it too. Closing any surface — done, back, timeout — fades down and reveals the keypad beneath, which makes the "terminal never rests on a personal screen" rule structural. |
| 3 | Auth persistence | Floor scope stores its non-expiring token in `localStorage` (device storage), per handoff §2. `401` anywhere ⇒ token revoked ⇒ clear + return to device sign-in. No refresh call ever happens under floor scope. Implemented centrally in `@beyo/api-client`/`@beyo/auth`, keyed off `app_scope === "floor"` — other apps' behavior is byte-identical to today. |
| 4 | Code entry | Keypad is **numeric, 4 digits**, auto-submitting on the 4th digit (design rule). The backend allows 4–16 char codes; the kiosk v1 only matches what a numeric 4-digit pad can express. **Email fallback** (confirmed by user 2026-07-29): a secondary affordance labeled **"Clock with email"** (NOT "Forgot your code?") opens a minimal email entry mode (same matcher, case-insensitive) because `clock_in_code` is `null` until a manager assigns one — without this path un-coded workers cannot use the kiosk at all. |
| 5 | Identification | Client-side roster matching per handoff §3: `GET /users?role=worker&compact=true&limit=200` cached via TanStack Query (`refetchInterval` 2 min + refetch on focus), trim input, code exact, email case-insensitive. The cache decides *who*, never *what state* — after confirm, `GET /current?user_id=…` is always fetched fresh before rendering the action button. |
| 6 | Kiosk theme | The kiosk visual system (warm paper `#FAF8F4`/`#F2EFE9`, ink `#1E1B17`, secondary `#8B8377`, tertiary `#A29A8E`, accent `#2F5D9E` as previewed in the images, success `#2E6A44`, error `#C0492E`) is registered as a **`--color-kiosk-*` token namespace in `@beyo/styles`** so kiosk components remain transplantable to any host app. Accent is one token — swappable later, not configurable in v1 UI. |
| 7 | Typography | Instrument Sans (UI) + IBM Plex Mono (all times/counts/deltas) self-hosted as `@font-face` in the floor app's `public/fonts` + `index.css`. Host apps that later mount the kiosk page must add the same two faces (documented in the kiosk package README section of Phase 7). |
| 8 | Realtime | **No socket in v1.** The handoff prescribes roster polling; no shift socket events exist for floor scope. `socket-registry.ts` ships empty; `RealtimeProvider` is not mounted. Revisit when backend emits shift events. |
| 9 | Device identity | "TERMINAL 04 · BAY B" is **device-local config** (a `deviceConfig` persisted store: terminal label, auto-return seconds default 12), set on first sign-in and editable from a settings surface opened via a deliberately non-obvious affordance (long-press on the header logo — must not be reachable by an idle tap). No backend involvement. |
| 10 | Declared states | **OUT of v1** (user decision 2026-07-29): the kiosk builds only the clock in/out interface. The backend's declared-states capability will be delivered later as separate pages the user develops in the future. `@beyo/worker-shifts` still wraps the declare/close endpoints (the domain package serves those future pages), but no kiosk UI consumes them, `@beyo/pause-reasons` is not a kiosk dependency, and **Phase 5 is shelved** — its plan and prompts remain on file for the future capability. |
| 11 | Build-ahead strategy | Endpoints that are ❌ in the handoff status table get **MSW handlers in `@beyo/worker-shifts/src/mocks/`** implementing the documented shapes (including 409/404/422 branches). The floor app runs against mocks via an env flag (`VITE_FLOOR_MOCKS=1`) until each backend phase flips ✅; Playwright always runs against the mocks. Design-ahead data (announcements, schedule, items, week, rate) flows through **adapter interfaces** that default to empty/null — screens render gracefully without them, and backend wiring later is a data change, not a UI change. |
| 12 | Responsiveness | iPad portrait is the primary design target (834×1194). One centred column — header / flexible middle / bottom-pinned action — capped at ~700–720px on iPad landscape and desktop; full-width with 20–24px side padding on phone with proportional scale-down (keys ~72–84px, hero numbers ~40px). Physical number-key + Enter/Backspace input works on desktop. Nothing interactive below 44px anywhere. |

## Package boundaries (binding on every child plan)

```
@beyo/worker-shifts   (packages/worker-shifts)   ← no UI, no surfaces, no kiosk knowledge
  - types.ts: zod schemas for FloorRosterUser, CurrentShift, ClockInResult,
    ClockOutResult, ClockOutAnalytics (timeline/segments/insights), DeclaredState,
    DeclareStateResult, CloseDeclaredStateResult — from handoff §3–§6 ONLY
  - api/: worker-shift-keys.ts, fetch-floor-roster.ts, use-floor-roster-query.ts,
    fetch-current-shift.ts, use-current-shift-query.ts
  - actions/: use-clock-in.ts, use-clock-out.ts, use-declare-state.ts,
    use-close-declared-state.ts
  - lib/: match-worker.ts (pure: trim, code exact, email ci), shift time helpers (pure)
  - mocks/: MSW handlers + fixtures for every ❌ endpoint (decision #11)
  - Depends on: @beyo/api-client, @beyo/lib (peers). NOT on @beyo/pause-reasons
    (the catalog stays owned by @beyo/pause-reasons; the kiosk composes the two).

@beyo/clock-kiosk     (packages/clock-kiosk)     ← the sellable kiosk experience
  - Kiosk flow state machine (store + controller + provider), auto-return timers
  - All kiosk screens/components (Claude-owned kit); identity-confirm and
    result/summary screens are `rise` surfaces over the always-mounted keypad
    page (decision #2)
  - surface-ids.ts + surfaces.ts: loadClockKioskPage() + the kiosk's rise-surface
    registrations (confirm, result) so host apps register everything centrally
    per 35_shared_packages.md §14
  - Adapter interfaces (KioskAdapters): announcements, scheduled shift, summary
    extras (items/week/rate) — defaulting to empty/null (decision #11)
  - Config via KioskProvider props: autoReturnSeconds, timeZone, adapters;
    host-owned chrome receives terminalLabel/workspaceName directly
  - Depends on: @beyo/worker-shifts, @beyo/ui, @beyo/hooks, @beyo/lib,
    @beyo/auth (peers)

@beyo/ui (extension, Phase 3)
  - New `rise` surface type: RiseSurface shell (fade-in slide-up enter,
    fade-out slide-down exit), registered additively in the SurfaceProvider
    renderer; existing surface types byte-untouched; noted in 28_surfaces_local.md

apps/floor-app        ← thin shell only
  - Floor-scope auth (device sign-in), device config store + settings surface,
    providers, router, surface registry, styling @source registration, fonts,
    PWA manifest. Owns zero kiosk logic — anything that would survive a port
    to another host app belongs in a package.
```

Rules:
- No package imports an app route, app store, or navigation function. App wiring flows through props/adapters/surface registrations per `35_shared_packages.md` §13–14.
- `@beyo/worker-shifts` must never import from `@beyo/clock-kiosk` (arrows point inward only), and never renders JSX.
- The floor app must remain deletable without touching kiosk behavior.
- Packages ship raw TS (`exports: { ".": "./src/index.ts" }`), `peerDependencies` only, no build step, per `35_shared_packages.md`.

## Design → data mapping (authoritative)

The five screenshots + `design_readme.md` are ground truth for look/interaction. This table is the single conversion contract between what the screens show and where the data comes from. **GAP** rows have no backend source yet — they render via adapters (decision #11) and are specified for the backend in `BACKEND_REQUIREMENTS_clock_kiosk_20260729.md`.

| Design element | Data source |
|---|---|
| Header workspace name ("Northbay Works") | auth store workspace/user context (sign-in response) |
| Header terminal line ("TERMINAL 04 · BAY B") | device config store (decision #9) |
| Header live clock + date | client clock, ticking each second, rendered in the workspace `time_zone` from sign-in |
| 4 code cells + keypad + validate-on-4th + shake-and-clear on miss | local matcher over the cached floor roster (`clock_in_code` exact); one generic error for code and email misses (anti-enumeration, handoff §3) |
| "Forgot your code?" (relabeled **"Clock with email"** — decision #4) | email entry mode |
| Confirm: photo, name | roster `profile_picture` (via `Avatar`/`BackendImage`), `username` |
| Confirm: role line ("Line 3 · Assembly") | roster `role` name; station/line + badge no. are **GAP** (until then: role name only) |
| Confirm: context row, clocked-out case ("Today's shift 07:00 – 15:30") | **GAP** — scheduled shift adapter; row hidden when adapter returns null |
| Confirm: context row, clocked-in case ("Clocked in at 06:58") | fresh `GET /current` → `shift_started_at`, localized |
| One primary action: Clock in (green) / Clock out (accent) | fresh `GET /current` → `clocked_in` decides which single button renders |
| Clock-in result: "Good morning/afternoon, {first}" | greeting from client clock in workspace tz; first name = first whitespace-separated token of `username` (pure helper) |
| Clock-in result: dark plate "CLOCKED IN AT hh:mm" | refetched `GET /current` → `shift_started_at` |
| Clock-in result: "SCHEDULED 07:00 – 15:30" on the plate | **GAP** — scheduled shift adapter; plate renders without the right column when null |
| "TODAY ON THE FLOOR" announcements (max 3, dated) | **GAP** — announcements adapter; whole section hidden when empty |
| Auto-return countdown ("Returning to the keypad in 9s") + Done | kiosk flow store, `autoReturnSeconds` from device config (default 12) |
| Clock-out summary: "Shift complete, {first}" + role + date | roster user + client date |
| Clock-out summary: dark plate "WORKED TODAY 8h 12m", IN/OUT | `clock-out` response `analytics.segments`: IN = `started_shift` marker start, OUT = `ended_shift` marker, worked = wall-clock span between them. `analytics: null` ⇒ plain success screen without any stats (handoff §5.1 hard rule) |
| "N active tasks were stopped" notice (additive, not in mock) | `clock-out` response `transitioned_steps` when > 0 (handoff says worth surfacing) |
| Items completed carousel (product images + units) | **GAP** — summary-extras adapter (`analytics.timeline.completed_count` exists as a total; the per-product breakdown does not) |
| This-week daily-hours bar chart vs 40h target | **GAP** — summary-extras adapter (analytics is day-scoped) |
| Rate today (units/hour) vs 5-day average | **GAP** — summary-extras adapter |
| Three factual insight rows with signed deltas | `analytics.insights` (code → copy mapping, `polarity` → delta color, may be `[]`; indicative-not-payroll caveat per handoff §5.1) |
| Declare flow | **Out of v1** (decision #10) — endpoints wrapped in `@beyo/worker-shifts` for the future declare pages; no kiosk UI |

Kiosk UX rules from the handoff that bind every phase: `409` is **normal flow** — never an error screen; on any 409 re-fetch `GET /current` and re-render the confirm step. After any await, re-render from `GET /current`, never from cached state. Every state returns to the keypad; auto-return is the safety net; the terminal must never be left on a personal screen.

## Backend route ownership by phase

| Route | Package | First used in phase | Live? |
|---|---|---|---|
| `POST /api/v1/auth/sign-in` (`app_scope: "floor"`) | `@beyo/auth` (extended) | 2 | ❌ (backend phase 5) |
| `POST /api/v1/auth/logout` | `@beyo/auth` | 2 | ❌ (backend phase 5) |
| `GET /api/v1/users?role=worker&compact=true&limit=200` (+floor fields) | `@beyo/worker-shifts` | 1 (wrapped), 4 (used) | ❌ (backend phase 6 for `clock_in_code`) |
| `GET /api/v1/worker-shifts/current?user_id=` | `@beyo/worker-shifts` | 1 (wrapped), 4 (used) | ❌ (backend phase 4) |
| `POST /api/v1/worker-shifts/clock-in` | `@beyo/worker-shifts` | 1 (wrapped), 4 (used) | ❌ (backend phase 4) |
| `POST /api/v1/worker-shifts/clock-out` (+`analytics`) | `@beyo/worker-shifts` | 1 (wrapped), 4 (used), 6 (analytics) | ❌ (phase 4; analytics phase 7) |
| `POST /api/v1/worker-shifts/declared-states` | `@beyo/worker-shifts` | 1 (wrapped; **no v1 UI** — future pages, decision #10) | ❌ (backend phase 3) |
| `POST /api/v1/worker-shifts/declared-states/close` | `@beyo/worker-shifts` | 1 (wrapped; **no v1 UI** — future pages, decision #10) | ❌ (backend phase 3) |
| `GET /api/v1/pause-reasons` | `@beyo/pause-reasons` (existing) | — (future declare pages; not a kiosk dependency) | ✅ live |
| `POST /worker-shifts/clock` (legacy toggle) | — | **never wrapped** | — |

Envelope/errors: standard `{ok, warnings, data}` + flat error string exactly per `04_api_client.md` + `04_api_client_local.md`; status semantics per handoff §8. No new parsing logic anywhere.

## Phased child implementation sequence

Each phase = one child plan in `plans/`, one Codex execution prompt in `prompts/execution/`, one Opus review prompt in `prompts/review/`. A phase is implemented and reviewed before the next begins. Codex and Opus start fresh sessions per phase; this master + the child plan carry all context they get.

| Phase | Child plan | Delivers |
|---|---|---|
| 1 | `PLAN_clock_kiosk_phase1_worker_shifts_package_20260729` | `@beyo/worker-shifts`: types for every handoff shape, query keys, API functions, query hooks, action hooks, pure roster matcher + time helpers, MSW build-ahead mocks with 409/404 branches, vitest suites, root script registration. No UI. |
| 2 | `PLAN_clock_kiosk_phase2_floor_auth_20260729` | Floor device auth: `"floor"` in scope unions, persisted-token path in `@beyo/api-client` (localStorage under floor scope, no refresh, 401 ⇒ revoked event), `@beyo/auth` AppScope + AuthProvider boot path for a persisted token, logout revocation. Zero behavior change for the three existing apps, proven by their auth tests. |
| 3 | `PLAN_clock_kiosk_phase3_floor_app_bootstrap_20260729` | `apps/floor-app`: workspace + typecheck registration, Vite/PWA/tsconfig/eslint/playwright config, fonts, `index.css` `@source` set + kiosk tokens in `@beyo/styles`, **the `rise` surface type in `@beyo/ui`** (decision #2), providers, router (`/sign-in` guest, `/` protected kiosk placeholder), device sign-in page, device config store + settings surface (rise type), kiosk chrome header (workspace, terminal label, ticking clock). Claude kit: header + sign-in/settings chrome + RiseSurface motion. |
| 4 | `PLAN_clock_kiosk_phase4_kiosk_core_flow_20260729` | `@beyo/clock-kiosk` package + the core loop: keypad screen (code cells, shake/clear, "Clock with email" mode), identity confirm (fresh `/current`, single action), clock-in result, minimal clock-out success (plain, pre-analytics), kiosk flow state machine (idle→match→confirm→act→result→auto-return), 409-as-refresh, desktop keyboard input, confirm/result as **rise surfaces** over the keypad, floor app mounts the kiosk page. Claude kit first: the full keypad/confirm/result component kit. |
| 5 | `PLAN_clock_kiosk_phase5_declared_states_20260729` | **SHELVED** (decision #10, user 2026-07-29) — declared states ship later as separate pages; the plan + prompts stay on file for that future capability and do not run in this sequence. |
| 6 | `PLAN_clock_kiosk_phase6_clock_out_summary_20260729` | The rich clock-out summary: analytics view-model mapping (worked/in/out from segments, insights rows, transitioned-steps notice, strict `analytics: null` degradation), summary-extras + scheduled-shift + announcements adapters (default empty), dark hero, items carousel, week bar chart, rate tile, insight rows — all render-complete against mock analytics + placeholder adapters. Claude kit first. |
| 7 | `PLAN_clock_kiosk_phase7_validation_polish_20260729` | Loading/empty/error/skeleton states everywhere, idle-screen resilience (device sleep/wake, clock drift, roster refetch), Playwright kiosk specs (mocked backend; tablet-primary + phone + desktop), vitest completion, public-API audit of both packages, root scripts, kiosk host-app integration README (fonts, tokens, adapters, surface registration), final design-fidelity pass against the five images. |

Dependencies: 1 and 2 are independent of each other; 3 needs 2; 4 needs 1+3; 6 needs 4; 7 needs 1–4 and 6. Phase 5 is shelved and outside the sequence (decision #10).

## Division of labor — Claude (design/components) vs. Codex (logic/assembly)

Same seam as the presentation capability (proven): presentational components are props/context-only; logic lives in hooks/stores/controllers/pure modules.

- **Claude owns component files** — DOM structure, Tailwind classes, kiosk tokens, motion, skeletons. For every UI phase Claude builds the phase's **component kit first**, props-first against mock data, matching `design_readme.md`'s visual system, before the Codex session starts. Each component ships with a typed prop contract (data in, callbacks out) and `data-testid`s.
- **Codex owns everything else** — types, api, hooks, stores, controllers, matching, mapping, persistence, tests — and the **assembly**: composing Claude's components into pages and feeding controller state through their props.
- **Codex treats Claude's components as read-only.** A prop-contract misfit is recorded in the child plan's Review log; purely additive optional props may be added without touching DOM/classes; anything structural or visual waits for Claude. Codex never restyles, never restructures markup, never edits class lists.
- Interaction-heavy components (keypad keys, code cells, shake animation, carousel, auto-return countdown) expose **injected callbacks only** (`onDigit`, `onDelete`, `onSubmit`, `onExpire`…); timers and matching live in the store/controller, not in components.

| Phase | Codex session | Claude kit session before it |
|---|---|---|
| 1 | everything (no UI) | — |
| 2 | everything (no UI) | — |
| 3 | scaffolding, auth wiring, device store, routing, rise-type registration plumbing, assembly | kit: kiosk chrome (header w/ ticking clock, page frame), `RiseSurface` shell motion, device sign-in card, settings surface chrome |
| 4 | flow store/controller, matcher wiring, queries/actions wiring, keyboard input, timers, assembly | kit: keypad screen (cells, keys, error shake, "Clock with email" mode), confirm screen (identity card, context row, action button), result screens (check hero, dark time plate, countdown footer) |
| 5 | — shelved (decision #10) | — |
| 6 | analytics mapping, adapters, null degradation, assembly | kit: summary hero, items carousel, week chart, rate tile, insight rows |
| 7 | tests, resilience, audit, scripts | design-fidelity + a11y half of the checklist |

Workflow per UI phase: Claude kit session (components committed, rendered with mock props for user design review) → user approves the kit → Codex session runs its execution prompt (kit exists, read-only) → Opus runs the review prompt → corrections routed (Codex for logic, Claude for visuals).

## Contracts and skills

### Contracts loaded (master-level; each child re-lists its own subset)

Core (always, per `task_system/frontend_contract_goal_mapping_guide.md`): `01_architecture.md` (+`_local`), `02_types.md`, `04_api_client.md` (+`_local`), `05_server_state.md`, `06_client_state.md`, `08_hooks.md`, `13_errors.md`, `15_feature_structure.md`.

Goal bundles + triggers:
- **New application bootstrap**: `14_styling.md` §14 (`@source` table, template `index.css`) — Phase 3.
- **New feature (CRUD)**: `16_feature_workflow.md`, `07_components.md`, `09_forms.md` (sign-in + description input only), `10_pages.md`, `11_routing.md`, `23_providers.md`, `24_dto.md`, `17_testing.md`, `34_runtime_validation.md` (+`_local`).
- Triggers: "auth/sign-in/token" → `12_auth.md` (+`_local`) (Phases 2–3); "modal/surface" → `28_surfaces.md` (+`_local`) — the new `rise` type extends the engine in Phase 3 and `28_surfaces_local.md` gains its entry then; `33_vaul_drawer.md` only if a vaul sheet is actually used; "animation" → `31_animations.md` (the rise enter/exit motion); "skeleton" → `32_loading_skeletons.md`; "responsive/breakpoint" → `27_responsive.md` (every UI phase); "env var" → `03_environment.md` (Phases 1, 3); "lazy load/package page" → `30_dynamic_loading.md` (+`_local`), `35_shared_packages.md` §14; "persistence/localStorage" → `26_persistence.md` (Phases 2–3); "playwright/runtime validation" → `34_runtime_validation_local.md` (Phases 4–7).
- Task-specific: `35_shared_packages.md` — package boundaries, package.json/tsconfig templates, loader functions. Central to every phase.

Excluded: `21_realtime.md` (decision #8 — no socket v1); `36_scroll_visibility.md` (no scroll-hidden chrome; summary page scrolls but hides nothing); `37_keyboard_aware_inputs.md` (kiosk keypad is its own UI; the one real text input — the "Clock with email" entry — lives on a non-scroll screen; re-evaluate in Phase 4 if the software keyboard overlaps it); `19_permissions.md` (the kiosk has no role-differentiated UI; the floor token is the permission).

### Domain grounding

This capability introduces a **new domain**. There is no existing `types.ts` for worker shifts — Phase 1 creates `packages/worker-shifts/src/types.ts` deriving every entity/field/enum **only** from `HANDOFF_TO_FRONTEND_worker_shift_floor_app_20260729.md` (never from contract example names). Entities: `FloorRosterUser`, `CurrentShift`, `ShiftState` (`idle | working | in_pause`), `SegmentState` (`started_shift | working | paused | idle | ended_shift`), `ClockInResult`, `ClockOutResult`, `ClockOutAnalytics` (+ `AnalyticsTimeline`, `AnalyticsSegment`, `AnalyticsInsight`), `DeclaredState`, `DeclareStateResult`, `CloseDeclaredStateResult`. Pause-reason shapes come from `@beyo/pause-reasons` (existing) — never redefined. Unknown extra keys inside `analytics` are ignored (additive contract, handoff §5.1).

### Targeted implementation references (read for "what exists," never for style)

| File | Read only for |
|---|---|
| `packages/pause-reasons/src/` — `index.ts`, `types.ts` | confirm catalog ownership so Phase 1 duplicates nothing (the future declare pages will compose it; the kiosk v1 never imports it) |
| `packages/api-client/src/auth-token.ts`, `packages/auth/src/{roles.ts, api/use-sign-in.ts, components/AuthProvider.tsx, store/auth.store.ts}` | exact current scope unions, token flow, boot sequence Phase 2 extends |
| `packages/stats/src/` — `package.json`, `tsconfig.json`, `surfaces.ts`, `api/worker-stats-keys.ts` | newest package scaffolding + key-factory + surfaces conventions (relational confirmation) |
| `apps/presentation-studio/.../src/app/` | the minimal no-tabs shell Phase 3 copies |
| `apps/managers-app/.../{vite.config.ts, index.css, package.json, src/main.tsx}` | app bootstrap wiring to replicate (Phase 3), alongside `14_styling.md` §14 |
| `packages/ui/src/components/primitives/{avatar, backend-image, ticking-timer, slide-stack, state-pill}` | prop surfaces of primitives the kit builds on |

### Skill selection

- No repo skill covers this; standard build order from `16_feature_workflow.md` applies per phase. Lifecycle per `skills/cross_cutting/plan_lifecycle_orchestrator/SKILL.md`. Playwright per `34_runtime_validation_local.md` (mobile/tablet first, then desktop).

## Acceptance criteria (master-level)

1. Every wrapped route matches the handoff exactly — method, path, roles, field names, enums, envelope; nothing invented; the legacy `/clock` toggle never wrapped.
2. Dependency arrows hold: `worker-shifts ← clock-kiosk`; `worker-shifts` has zero UI imports; the floor app contains no kiosk logic; no package imports app internals.
3. The kiosk works end-to-end against the MSW mocks with the backend fully offline, and against a live backend by flipping one env flag — no code change.
4. Handoff UX rules verifiably implemented: fresh `GET /current` before every action render; 409 → friendly state refresh, never an error screen; `analytics: null` → plain success screen; every screen auto-returns to the keypad; one generic no-match message; the code never leaves the device (identification is local; no identify endpoint is called).
5. Floor auth: token persisted on device, survives reload without any refresh call; `401` anywhere returns the device to sign-in; the three existing apps' auth behavior is unchanged (their tests prove it).
6. Design fidelity: the five screens match the images across iPad portrait (primary), phone, and desktop per the responsive intent; all interactive targets ≥44px; times/counts/deltas render in the mono face.
7. Every GAP row in the mapping table renders through an adapter with a graceful empty state, and `BACKEND_REQUIREMENTS_clock_kiosk_20260729.md` documents each one for the backend team.
8. All child plans keep phase boundaries; no phase implements a later phase's responsibilities.
9. This master implements no code and modifies neither the design nor the backend handoff.

## Risks and mitigations

- Risk: floor-scope changes to `@beyo/api-client`/`@beyo/auth` regress the three live apps.
  Mitigation: Phase 2 is logic-only, gated on the existing auth test suites + full root typecheck; the persisted path is strictly `app_scope === "floor"`-conditional; review prompt orders a diff-level check of the non-floor code paths.
- Risk: backend phases land with contract drift against the handoff.
  Mitigation: the handoff is edit-first authority; mocks are keyed to its shapes; Phase 7 re-runs flows against any endpoint that has flipped ✅ before removing its mock.
- Risk: shared-device token in localStorage is readable by anyone at the terminal.
  Mitigation: accepted by the backend contract (device is the trust anchor; the code identifies, never authenticates); logout is permanent revocation; no other scope ever persists.
- Risk: auto-return timers + async actions race (action resolves after return-to-keypad).
  Mitigation: the flow store owns a session id per kiosk interaction; stale async results against a different session id are dropped; unit tests cover the race in Phase 4.
- Risk: 200-worker roster cap (`limit=200`) silently truncates larger workspaces.
  Mitigation: surfaced in `BACKEND_REQUIREMENTS` (pagination or raised cap); v1 logs a console warning when `users.length === 200`.
- Risk: the declare flow has no design ground truth. — **Closed**: declares removed from v1 (decision #10, user 2026-07-29); the risk transfers to the future declare-pages capability.
- Risk: the new `rise` surface type destabilizes the shared SurfaceProvider engine used by three live apps.
  Mitigation: the type is registered additively (new shell component + renderer case); existing surface-type code paths untouched; `@beyo/ui` surface tests extended in Phase 3 and the reviewer diff-audits the renderer change.

## Review log

- 2026-07-29 user: decisions reviewed. #4 confirmed with amendment — the email fallback affordance is labeled **"Clock with email"**, not "Forgot your code?". #10 overturned — declared states are OUT of v1 (future separate pages); Phase 5 shelved. #2 amended — the kiosk uses a new **`rise` surface type** (fade-in slide-up / fade-out slide-down) **everywhere**: flow screens are rise surfaces over the always-mounted keypad; no SlideStack in the kiosk. All other defaults (#1, #3, #5–#9, #11, #12) approved as written.
- 2026-07-29 Claude (Fable): master + child plans + prompts updated to reflect the three amendments; master status → `approved`.
- 2026-07-29 Claude (Fable): Phase 2 review M1 (sign-out's unwrapped logout POST can leave the persisted floor token behind) resolved by orchestrator decision: **floor-gated `finally`** in `@beyo/auth` sign-out — unconditional local revocation under floor scope only; implemented in Phase 3 (its plan's criterion 5 amended). Opus's universal-teardown recommendation recorded as a post-capability proposal, deliberately NOT taken now (live-app behavior change).
- 2026-07-29 Codex: Phase 1 completed. Created `@beyo/worker-shifts` with handoff-grounded schemas, query/action APIs, pure helpers, and stateful build-ahead MSW handlers. Validation passed (`npm run typecheck`; `npm run test:worker-shifts`, 32/32). Summary: `docs/architecture/implemented_summaries/SUMMARY_clock_kiosk_phase1_worker_shifts_package_20260729.md`; archived child: `docs/architecture/archives/implementation/PLAN_clock_kiosk_phase1_worker_shifts_package_20260729.md`. Master remains `approved` and unmoved.
- 2026-07-29 Opus (Phase 1 review): verdict **defects found** — 3 blocking, 2 medium, 3 low. Corrections plan: `plans/PLAN_clock_kiosk_phase1_corrections_20260729.md` (all findings routed to Codex; Phase 1 has no visuals). Validation independently re-run and confirmed: `npm run typecheck` green, `npm run test:worker-shifts` green (5 files / 32 tests) — the summary's claims are accurate. Verified clean: no JSX/react-dom in the package, legacy `POST /worker-shifts/clock` absent everywhere, no optimistic updates in any action hook, no `@beyo/pause-reasons` import or catalog DTO re-export, every 409/404/422 branch and both analytics variants present in the mocks with fixtures parsed through the schemas, `match-worker`/`shift-time` pure with injectable `now` and no module state, root `package.json` registration correct, no `apps/` changes, plan archived + moved and master otherwise untouched. **Blocking (master AC #1 — contract fidelity):** (F1) `FloorRosterUserSchema.profile_picture` is non-nullable while the same `/users` payload is nullable in eight other packages — one worker without a photo throws `invalid_response` and kills roster matching for the whole terminal; (F2) `image_url` non-nullable in both embedded and analytics pause-reason schemas, contradicting the live catalog owner `@beyo/pause-reasons` and `@beyo/stats`'s identical lookup shape; (F3) analytics schemas require keys that `@beyo/stats` — same shapes per handoff §5.1, built against the live backend — already treats as optional/defaulted, so a successful clock-out will reject as a 502 the moment backend phase 7 flips analytics on. **Medium:** (F4) `src/api/schemas.ts` reimplements `@beyo/lib`'s `ApiEnvelopeSchema` (declared peer dep, never imported) against master line 144; (F5) MSW handlers are re-exported from the package root barrel with `msw` as a runtime peer dependency, which per `35_shared_packages.md` puts mocks in the main chunk and makes decision #11's `VITE_FLOOR_MOCKS` flag un-splittable. **Low:** (F6) disabled current-shift query parks on the list-level namespace key; (F7) the fetch-fresh pattern is undocumented in `index.ts` (phase criterion 4); (F8) `MockReason` restates the pause-reason catalog shape in handler code.
- 2026-07-29 Codex: Phase 1 corrections completed; F1–F8 are fixed. Validation passed (`npm run typecheck`; `npm run test:worker-shifts`, 5 files / 36 tests); the plan consumer grep returned no matches. Summary: `docs/architecture/implemented_summaries/SUMMARY_clock_kiosk_phase1_corrections_20260729.md`; archived corrections plan: `docs/architecture/archives/implementation/PLAN_clock_kiosk_phase1_corrections_20260729.md`. Master remains `approved` and unmoved.
- 2026-07-29 Codex: Phase 2 completed. Added floor-only device-token persistence and boot restore in `@beyo/api-client`, widened shared auth scope/claims/sign-in parsing, and preserved the existing expiry-event/logout paths. Validation passed (`npm run typecheck`; `npm run test:api-client`, 3/3; `npm run test:auth`, 1/1). The floor 401 MSW request log was exactly `GET /api/v1/protected` and asserted **zero refresh calls**; all four non-floor scopes asserted zero storage reads/writes/removals. Summary: `docs/architecture/implemented_summaries/SUMMARY_clock_kiosk_phase2_floor_auth_20260729.md`; archived child: `docs/architecture/archives/implementation/PLAN_clock_kiosk_phase2_floor_auth_20260729.md`. No `apps/*` source changed; master remains `approved` and unmoved.
- 2026-07-29 Opus (Phase 1 re-review, post-corrections): verdict **pass-with-notes** — F1–F8 all independently verified closed, no regressions, no new defects. Validation re-run by the reviewer: `npm run typecheck` green across all four apps and every package in the chain (the transient `SignInForm.tsx:85` failure the corrections summary reported from concurrent in-flight Phase 2 edits is resolved); `npm run test:worker-shifts` green, 5 files / 36 tests — the four added cases are substantive, not tautological. Closures: F1 `profile_picture` nullable (`types.ts:30`) with a null-photo roster fixture (`mocks/fixtures.ts:27`) exercised end-to-end through `fetchFloorRoster` (`worker-shifts-api.test.ts:105`); F2 `image_url` nullable in both embedded and analytics pause-reason schemas (`types.ts:6`, `types.ts:11`) and `reason_text` now `.nullable().optional()` (`types.ts:56`), with a null catalog image reaching the declare path via the mock (`mocks/handlers.ts:31`); F3 analytics tolerance aligned to the live-proven `@beyo/stats` shapes — `date_from`/`date_to` optional, `pause_by_reason`/`segments`/`segments_truncated`/`manually_recorded`/`steps`/`insights`/`pause_reasons` defaulted, `.passthrough()` retained everywhere (`types.ts:66-121`), with a minimal-payload parse test asserting each default and the full §5.1 example still round-tripping byte-for-byte; F4 `api/schemas.ts` deleted and all six HTTP call sites on `ApiEnvelopeSchema` from `@beyo/lib`, making the declared peer dep real; F5 `"./mocks"` subpath export added, `msw` dropped from `peerDependencies`, root barrel mock re-export removed (`package.json:8`, `index.ts`) — repo-wide grep confirms no consumer imports mocks from the root barrel, including the newly scaffolded `@beyo/clock-kiosk`; F6 disabled query moved to `current({ user_id: "" })`, off the list-namespace key (`use-current-shift-query.ts:13`); F7 fetch-fresh pattern documented in the barrel (`index.ts:53-54`); F8 `MockReason` catalog type removed, lookup inferred from the literal. Regression sweep clean: no JSX/react-dom outside the test wrapper's `createElement`, no `setQueryData` in any action hook, legacy `POST /worker-shifts/clock` still absent repo-wide, no `@beyo/pause-reasons` import, no module state in `lib/`. **Note (non-blocking):** `msw` is now declared nowhere in `packages/worker-shifts/package.json` while `src/mocks/handlers.ts` imports it and `./mocks` is a public subpath entry; it resolves today via the root devDependency hoist, but a package-level `devDependencies: { "msw": "^2.15.0" }` would declare the dependency honestly — `packages/shopify` sets that precedent with `fake-indexeddb`. Phase 1 is blessed; no corrections plan created.
- 2026-07-29 Opus (Phase 2 review): verdict **pass-with-notes** — no severity-1 defect; 1 medium and 3 low findings, **no corrections plan created** (see routing note below). **Checklist #1 (non-floor invariance) — PASS, and this is the load-bearing conclusion of the review.** Every changed branch was walked: (a) `setAccessToken` — `if (_authScope !== 'floor') return` (`auth-token.ts:19`) precedes all three storage calls, and the new optional `scope` param is only ever passed by `use-sign-in.ts:101`, where `setAuthScope(appScope)` had already run at line 90, so the assignment is a re-assignment of the identical value; (b) `refreshAccessToken` — the floor branch (`auth-token.ts:90-93`) sits ahead of the `_refreshPromise` single-flight and the `!_authScope` guard, both of which are reached unchanged under every other scope; (c) `initSession` — the added `setAuthScope(scope)` (`auth-token.ts:137`) is redundant for non-floor because the pre-existing `return refreshAccessToken(scope)` already performed it, making the non-floor path byte-equivalent; (d) `TokenClaims.app_scope` (`auth-token.ts:58`) and `AppScope.Floor` (`roles.ts:15`) are pure additive widenings — repo-wide grep confirms `AppScope` is never enumerated (`Object.values/keys/entries`) and nothing switches exhaustively on `app_scope`, so no live-app UI gains a "floor" option; (e) `AuthProvider`'s `appScope: string → AuthAppScope` (`AuthProvider.tsx:25`) is a *narrowing*, not a widening — verified safe because all four mount sites pass string literals (`managers/src/app/RootRoute.tsx:30`, `sellers/src/app/RootRoute.tsx:30`, `workers/src/features/auth/AuthProvider.tsx:13`, `presentation-studio/src/app/RootRoute.tsx:9`) and `SignInForm.appScope` was deliberately left as `string` (summary "Deviations" #1). Conclusion: **zero behavioral diff is reachable under `manager`/`worker`/`seller`.** Checklist #2 — PASS: the floor 401 path calls no `fetch`; the gate lives inside `refreshAccessToken` itself, so *both* callers (`api-client.ts:130` and `RealtimeProvider.tsx:94`) are covered by one branch; memory and storage clear via `setAccessToken(null)`; the **existing** `auth:session-expired` is dispatched from the unchanged `api-client.ts:137` — no new event name and no parallel sign-out path exist repo-wide. Checklist #3 — PASS: exactly one key (`beyo.floor.access_token`), three storage calls total, all in `auth-token.ts`, all floor-gated or boot-only; `try/catch` around each; the token appears only in the `Authorization` header (`api-client.ts:124`) — never in a URL (`_executeRefresh` interpolates only `scope`) and never logged (the package's sole `console.*` is `env.ts:10`). Checklist #4 — PASS: `initSession('floor')` restores and returns presence, `AuthProvider` then hydrates via `/users/me`, and a 401 there routes through the floor branch → storage+memory cleared → `auth:session-expired` → `clearAuth()` + navigate to `signInRoute`; the expiry listener is registered synchronously before the async continuation can fire. Checklist #5 — PASS: the zero-refresh assertions genuinely inspect the MSW log (`auth-token.test.ts:56` `requestLog).toEqual([])` on boot, `:101-102` `toEqual(['GET /api/v1/protected'])` plus an explicit `/auth/refresh` count of 0 on revocation, with `onUnhandledRequest: 'error'`), and the never-touches-storage test covers **four** non-floor scopes (`admin`/`manager`/`worker`/`seller`) with `Storage.prototype` spies on `getItem`/`setItem`/`removeItem`, positively controlled by asserting 4 refresh calls did happen; `restoreMocks: true` is set in both new vitest configs, so the spies do not leak between files. Checklist #6 — validation independently re-run by the reviewer: `npm run typecheck` exit 0 (all four app workspaces + the full registered package chain), `npm run test:api-client` 1 file / 3 tests green, `npm run test:auth` 1 file / 1 test green — the summary's evidence is accurate. Checklist #7 — PASS: summary present, child plan moved to `archives/implementation/` with `status: archived`, `ARCHIVE_…_1609.md` record written, master Review log entry present **and it does state the zero-refresh result**; master otherwise unmodified; no `apps/*` source changed. **Medium (M1)** — `packages/auth/src/api/use-sign-out.ts:15-18`: `await apiClient.post('/api/v1/auth/logout', …)` is not wrapped, so any failure (offline kiosk, 5xx, or a 401 that resolves before `clearAuth`) short-circuits `setAccessToken(null)` and `useAuthStore.clearAuth()` while `onSettled` still fires `onSignedOut()`. Under floor this means a failed logout leaves the **non-expiring** device token in `localStorage` and memory on a shared shop-floor terminal, and the next boot silently re-restores it — contradicting phase criterion 5 ("Sign-out … clears storage + memory + query cache") and this master's risk mitigation "logout is permanent revocation". The code itself is pre-existing and `use-sign-out.ts` is not in the phase plan's Scope list, so this is a latent gap that floor persistence newly makes consequential rather than a regression Codex introduced. **Routing (why no corrections plan):** the natural fix — moving the local teardown into a `finally` — changes sign-out behavior for the three live apps, which Phase 2's own charter forbids ("Zero behavior change for the three existing apps"). It therefore needs a user decision and belongs to Phase 3, which owns the floor app's device sign-out wiring: either accept the shared `finally` (recommended — unconditional local revocation is the correct default everywhere) or have the floor app clear the key itself on sign-out failure. **Low (L1)** — `auth-token.ts:139-148`: the floor restore returns `true` for any non-empty stored string; `AuthProvider.tsx:44-45` then bails at `if (!claims) return` without clearing, so a corrupt/truncated persisted JWT is re-restored on every boot and attached as a `Bearer` header until some call 401s. It converges (next sign-in overwrites), and the summary consciously declined to invent this requirement, but validating via `decodeTokenClaims()` in the restore path and clearing on failure would make boot deterministic. **Low (L2)** — no test covers floor sign-out clearing the key; criterion 6 did not enumerate one, but it is the other half of criterion 5 and would have surfaced M1. **Low (L3)** — `refreshAccessToken` guards on `_authScope === 'floor'` (`:90`) while `initSession` guards on `scope === 'floor'` (`:139`); equivalent only because `initSession` calls `setAuthScope` first — a single `isFloorScope()` predicate would make the invariant explicit. Phase 2 is blessed; Phase 3 may proceed, carrying M1.

- 2026-07-29 Codex (Phase 3 implementation): completed and archived `PLAN_clock_kiosk_phase3_floor_app_bootstrap_20260729`. Added the registered `managerbeyo-app-floor` thin host with `AuthProvider appScope="floor"`, guest sign-in, protected empty kiosk placeholder, persisted device config, workspace-time-zone clock, 600 ms long-press-only rise settings, confirmed logout with unconditional local floor teardown, PWA, and self-hosted fonts. Registered the Claude-owned `RiseSurface` additively in `@beyo/ui`; existing surface shell implementations remain unchanged, and three existing route-frame state types were narrowed to their routed types so root typecheck stays exhaustive. Final `@source`: `ui`, `hooks`, `auth`, `lib`, `clock-kiosk` (the last two are required by §14 because they are consumed and class-bearing). Validation: root `npm install` exit 0, root typecheck exit 0, floor unit 4 files / 6 tests, UI 29 files / 162 tests, desktop `floor-bootstrap` Playwright 1/1, floor build and lint green. Summary: `docs/architecture/implemented_summaries/SUMMARY_clock_kiosk_phase3_floor_app_bootstrap_20260729.md`; archive: `docs/architecture/archives/ARCHIVE_clock_kiosk_phase3_floor_app_bootstrap_20260729_1642.md`.
- 2026-07-29 Codex (Phase 3 M1 amendment): replaced the floor host's callback-only teardown with the orchestrator-approved shared-auth implementation. `useSignOutMutation({ appScope: "floor" })` selects a floor-only logout function that attempts `POST /api/v1/auth/logout` and guarantees persisted floor token, in-memory token, auth-store, and notification-state cleanup in `finally`; the existing `onSettled` clears the query cache and closes settings so the auth guard lands on sign-in. The pre-existing non-floor sign-out function/default hook path remains unchanged. Regression coverage proves logout API failure clears `beyo.floor.access_token` and proves non-floor failure invariance. Amendment validation passed: `npm run test:auth` 2 files / 3 tests, root typecheck, floor unit 4 files / 6 tests, and desktop `floor-bootstrap` Playwright 1/1. Optional Phase 2 L1 was not implemented.

- 2026-07-29 Opus (Phase 3 review): verdict **pass-with-notes** — no blocking defect; 2 medium and 9 low/note findings. Corrections plan: `plans/PLAN_clock_kiosk_phase3_corrections_20260729.md` (C1–C8 → Codex, C9–C11 → Claude). **Phase 4 is not gated on it.** Validation independently re-run by the reviewer and confirmed accurate: `npm run typecheck` exit 0 (all five app workspaces + the registered package chain); `npm run test:ui` 29 files / 162 tests; `npm run test:auth` 2 files / 3 tests; `npm run test:api-client` 1 file / 3 tests; floor `npm run test:unit` 4 files / 6 tests; `npx playwright test --grep floor-bootstrap --project=desktop` 1/1; floor `lint` exit 0. **Checklist 2b (`rise` type safety) — PASS, and this is the load-bearing conclusion.** The `@beyo/ui` engine diff is exactly three additive lines (`SurfaceProvider.tsx:16` import, `:20` union member, `:252` shell-map entry); `SlidePageSurface`/`BottomSheetSurface`/`ModalSurface` and every existing code path are byte-untouched, and `SurfaceShellProps` already carried `showBackdrop`, so no shared type widened for rise's sake. `RiseSurface` reuses the engine's own idioms (Escape close, focus-reclaim on mount, `SurfaceHeaderContext` with `requestClose` live) and additionally gates its Escape listener on `isTopmost`, which `ModalSurface` does not. Coverage is real, not nominal: `SurfaceProvider.rise.test.tsx` (2 tests — backdrop render + shared close lifecycle; two-deep stacking with `inert` on the covered surface and not the top) was verified to actually be collected by the `@beyo/ui` vitest config, and `surface-history.test.ts` gained a rise open/close history-depth case (9 tests in that file now). `28_surfaces_local.md` gained the `rise` row, the shell-map snippet, and a behavior section. The three live apps' `SurfaceRouteFrame.tsx` change is **type-only and provably runtime-inert**: `SurfaceType` is re-exported from `@beyo/ui` through each app's local provider, so widening the union made `SURFACE_SHELLS[state.surface]` non-exhaustive against a 3-key `as const` map; narrowing `Exclude<SurfaceType,'page'>` to `Exclude<SurfaceType,'page'|'rise'>` restores the compile error surface, and the pre-existing `if (!Shell) return <Outlet/>` guard already handled an unmapped value identically. Checklist #1 — PASS: a repo-wide grep of the app for matching/shift/roster/keypad/declared/pause-reason/socket/worker-shifts returns **zero** hits; the only timers are the header clock (`use-kiosk-clock.ts:29`) and the long-press (`use-device-settings-long-press.ts:20`); `/` renders an empty `aria-hidden` placeholder inside real chrome. Checklist #2 — PASS: no tabs, no `TabSlideStack`, no `SlideStack`, no `RealtimeProvider`; `SurfaceProvider registry={surfaceRegistry}` at `RootRoute.tsx:10` with device-settings registered under `surface: "rise"`. Checklist #3 — PASS: the app imports exactly `@beyo/{auth,clock-kiosk,hooks,lib,ui}` (+ `@beyo/styles` as CSS) and `index.css` lists exactly those five `@source` lines — no missing entry, none extra. Checklist #4 — PASS: the `@beyo/styles` diff adds only new lines; **no existing token is modified or removed** (diff-verified). Checklist #5 — kit integrity could not be established by diff (the kit was never committed; `packages/clock-kiosk` is entirely untracked), so it was established by inspection instead: every kit component is props-only with no state, no effects, and no imports beyond `react`/`@beyo/lib`'s `cn`; `KioskHeader` spreads `identitySlotProps` **before** its own `className`/`data-testid`, so a host cannot displace kit styling; the barrel carries Codex's read-only banner. Checklist #6 — PASS: settings open only through the 600 ms hold (no `onClick`/tap path anywhere; covered by a unit test asserting an idle tap does nothing), logout sits behind an inline confirm with a separate device-config-wipe checkbox, and Phase 2's **M1 is genuinely closed** — `signOutFloor`'s `finally` (`use-sign-out.ts:30-34`) clears token + auth store + toast tracking on server failure, is selected only when `appScope === AppScope.Floor`, leaves the non-floor `signOut` byte-unchanged, and is covered by *both* halves the criterion asked for (`use-sign-out.test.tsx:51` failure-clears-storage, `:86` non-floor invariance). Checklist #7 — PASS: workspaces glob `apps/floor-app/*`, floor added to the typecheck chain, dev port 5175, manifest standalone + `orientation: any` with no push code anywhere. Checklist #9 — PASS: summary, `ARCHIVE_…_1642.md`, archived child plan, intention-plan progress line, and master entry all present; master otherwise unmodified. **Medium (C1)** — `playwright.config.ts:14-21` declares only a `desktop` project, while `34_runtime_validation_local.md` mandates `mobile` **and** `desktop` and orders mobile run first; worse for this capability, master decision #12 makes **iPad portrait the primary design target** and no project exercises it, so the kiosk's primary viewport is never validated. Phase 3's own criterion 9 asked only for desktop, so this is a contract gap the child plan under-specified rather than a Codex deviation. **Medium (C2)** — `autoReturnSeconds` is parsed but never range-checked: `DeviceSettingsPage.tsx:50` persists any integer, and `PersistedDeviceConfigSchema` (`device-config.store.ts:9`) is `z.number().int()`, so `0` or a negative survives both save and rehydrate. That value is the input to Phase 4's auto-return safety net (decision #9), i.e. the mechanism enforcing "the terminal must never be left on a personal screen" — the one device setting where an unvalidated number has a real-world consequence. **Low:** (C3) `@beyo/clock-kiosk` is a floor-app runtime dependency but has no root typecheck entry, unlike the sibling `@beyo/worker-shifts` added in Phase 1 — it is typechecked only incidentally through the app's program; (C4) `14_styling.md` §14's authoritative table was not updated for `@beyo/clock-kiosk`, which that section's own step 2 requires; (C5) floor `tsconfig.app.json:41` excludes `src/**/*.test.ts(x)`, so the four new test files are never typechecked while the three sibling apps include theirs; (C6) `@beyo/notifications` and `@beyo/api-client` are declared dependencies that no app source imports; (C7) the revoked-device note (`SignInPage.tsx:62`) is inferred from a non-empty persisted `terminalLabel` rather than from the `auth:session-expired` event, and criterion 6's revoked-device landing has no unit or e2e coverage; (C8) `registerType: "prompt"` asks an unattended kiosk for a service-worker update confirmation no one is there to give; (C9) `RiseSurface.tsx:92-96` pairs `role="dialog"` + `aria-modal="true"` with no accessible name where `ModalSurface` uses `aria-labelledby`; (C10) `.kiosk-shake` (`packages/styles/src/index.css:84-110`) is Phase 4 keypad-error styling landed in Phase 3, against AC #8. **Note (C11)** `KioskKitShowcase` is exported from the kit's public barrel. **Note on evidence:** the summary's Validation section omits `test:auth`/`test:api-client` even though the phase modified shared `@beyo/auth` sign-out; the intention-plan entry does record "auth 3/3", and the reviewer re-ran both green — a reporting gap, not a validation gap. Phase 3 is blessed; Phase 4 may proceed.

- 2026-07-29 Codex (Phase 3 corrections): completed C1–C8 and archived `PLAN_clock_kiosk_phase3_corrections_20260729`. Playwright now covers iPhone 14 Pro mobile, primary iPad portrait 834×1194 tablet, and 1440×900 desktop; the unchanged bootstrap case passed 1/1 on each. Auto-return is constrained by one exported 4–120 range at the input, save, and persisted-schema boundaries, with rejected-save and corrupt-rehydrate tests. Root typecheck explicitly includes `@beyo/clock-kiosk` and floor source tests; the styling authority table and direct floor dependencies are corrected; PWA registration uses `autoUpdate`. The existing sole `AuthProvider` expiry listener gained an optional callback used only by the floor host to record a session-scoped revocation flag; tablet Playwright proved stored floor token → authenticated `/users/me` → mocked 401 → token cleared → `/sign-in` note visible. Full validation passed: root typecheck; floor unit 5 files / 8 tests; UI 29 / 162; auth 2 / 3; api-client 1 / 3; the four required Playwright commands; floor lint and build. Claude's C9/C11 remain complete; C10 retains its approved no-removal disposition because Phase 4 `CodeCells` consumes `.kiosk-shake`. Summary: `docs/architecture/implemented_summaries/SUMMARY_clock_kiosk_phase3_corrections_20260729.md`; archive record: `docs/architecture/archives/ARCHIVE_clock_kiosk_phase3_corrections_20260729_1930.md`. Master remains `approved` and unmoved.

- 2026-07-29 Codex (Phase 4 implementation): completed and archived `PLAN_clock_kiosk_phase4_kiosk_core_flow_20260729`. `@beyo/clock-kiosk` now owns the session-scoped keypad → fresh confirm → explicit action → result → reset flow; the floor route mounts its loader and centrally merges its two `rise` surfaces. All five invariants are covered: local roster identity plus fresh `/current` action state; 409 correction back to one confirm action; one cleared-keypad reset boundary for back/done/error/result timeout/30-second confirm inactivity; one generic local code/email miss; stale-session async result suppression. Clock-out analytics remain parsed but unrendered, while `transitioned_steps` appears on the plain success screen. `VITE_FLOOR_MOCKS=1` boots Phase 1 handlers plus floor auth mocks and was smoke-tested without route interception. Validation passed: root typecheck; kiosk 2 files / 9 tests; worker-shifts 5 / 36; Playwright mobile 5/5 then desktop 5/5; floor production build and diff check. No Claude-owned component file was edited. Summary: `docs/architecture/implemented_summaries/SUMMARY_clock_kiosk_phase4_kiosk_core_flow_20260729.md`; archive record: `docs/architecture/archives/ARCHIVE_clock_kiosk_phase4_kiosk_core_flow_20260729_1935.md`. Master remains `approved` and unmoved; Phase 6 is next.

- 2026-07-30 Opus (Phase 4 review): verdict **defects found** — 2 blocking, 2 high, 2 medium, 7 low, 2 notes. Corrections plan: `plans/PLAN_clock_kiosk_phase4_corrections_20260730.md` (C1–C15 → Codex; Claude only if C4's option (b) is chosen). **Phase 6 should not start until C1/C2 land** — both break the floor app outside the kiosk loop. Validation independently re-run by the reviewer and confirmed accurate: `npm run typecheck` exit 0; `npm run test:clock-kiosk` 2 files / 9 tests; `npm run test:worker-shifts` 5 files / 36 tests; `npx playwright test --grep clock-kiosk` 5/5 on `mobile`, 5/5 on `desktop`, **and 5/5 on `tablet`** (the iPad-portrait primary target, which Phase 4 itself never ran — evidence gap C15, not a defect); floor production build exit 0. **The five invariants, each dispositioned.** (1) *Fresh `/current` after every match and every await* — **PASS**: `submitMatch` matches locally then calls `fetchCurrentShift` directly (never the cached query), and both action branches re-fetch after the mutation await before any result renders (`use-kiosk-flow.controller.ts:87,226,241`); the confirm button is `disabled` and reads "One moment…" until `current !== null`, so no action is ever rendered from absent or cached state; covered by `wires fourth-digit local matching to a fresh current-state read` and by both Playwright journeys asserting exactly two `/current` requests. (2) *409 → silent refetch → confirm re-render* — **PASS, and verified against the real error shape**: `isHttpConflict` duck-types `status === 409`, which `ApiRequestError` (`api-client.ts:12-22`) satisfies; the reviewer drove a genuine `409` response through the running app and observed the confirm surface re-render as "Clock out now" with **no** result surface, no keypad bounce and no error UI, matching the unit test `treats 409 as a fresh state correction…` (note C14: that test rejects with a bare `{status:409}` rather than an `ApiRequestError`). (3) *Every path returns to a cleared keypad* — **PASS on state, FAIL on presentation**: one `returnToKeypad` boundary resets the store and closes both surfaces for back/done/result-countdown/30-s confirm inactivity (`:79-82,277-294`), proven by the store reset test, the 4-second result test, the 30-second confirm test and the Playwright timeout journey; but the reset runs **before** the surfaces close, so the rise panels animate out empty (C5), and the keypad page returns `null` while any surface is open, so nothing is mounted beneath to be revealed (C4). (4) *One generic no-match message; the code never leaves the device* — **PASS**: `matchWorker` is local, `rejectMatch` clears both identifier fields and raises one message ("No worker matches this code or email", handoff §3 verbatim); the network layer contains no identify call and the typed value appears in no request, URL or log — grep of the package shows zero `console.*`, and the wrong-code Playwright case asserts zero `/current` traffic. (5) *Stale session-id results are dropped* — **PASS**: every async continuation re-checks `isSessionActive` against the store's live `sessionId` before applying state and `reset()` mints a new id; the race test `drops a fresh-current response after reset invalidates its session` was run and passes, as does the store's stale-transition test. **Also verified clean:** kit integrity — `git diff packages/clock-kiosk/src/components/` is **empty** (all 22 kit files byte-untouched since the kit commit; no additive prop was needed) and the kit contains no timer, fetch, matcher or `Date` anywhere; package boundaries — `clock-kiosk` imports only `@beyo/worker-shifts`, `@beyo/ui`, `react`, `zustand` and its own relatives, zero app imports, and `@beyo/worker-shifts`/`@beyo/ui`/`@beyo/styles`/`@beyo/auth`/`@beyo/api-client` are unmodified in the working tree; surface centralization — confirm and result are registered as `rise` in the package's `surfaces.ts` and merged into the floor app's single `surface-registry.ts`, with no SlideStack and no ad-hoc overlay anywhere; phase boundaries — `analytics` parsed and unrendered (Playwright asserts the mock's `completed_count` never appears), no declare UI, adapters default to empty/null; desktop input — `page.keyboard.type('4821')` completes the journey on all three projects; lifecycle bookkeeping — summary, archive record, archived child plan, and a master diff of exactly the one Review-log entry. **Blocking (C1)** — the kiosk's global `keydown` listener (`use-kiosk-flow.controller.ts:296-318`) is mounted app-wide, because `KioskProvider` sits above the router `Outlet` in `RootRoute.tsx:17-21`, and it `preventDefault()`s every 0–9/Backspace/Enter with no event-target check. Measured on the running app: on `/sign-in` typing `TERMINAL 04` into the terminal-label field yields `"TERMINAL "`, a manager password `Passw0rd123` yields `"Passwrd"`, and `floor42@shop.com` yields `"floor@shop.com"`; on the device-settings surface the auto-return field cannot be changed at all (stays `12` when `30` is typed) and four typed digits open the identity-confirm surface **over** the settings panel. The device cannot be set up from a keyboard — and an iPad's software keyboard emits the same `keydown`s. **Blocking (C2)** — the same provider placement mounts `useFloorRosterQuery` on the unauthenticated sign-in screen. Measured: `GET /api/v1/users?role=worker&compact=true&limit=200` is issued on `/sign-in` with no token; a `401` there runs the Phase 2 revocation path (`setAccessToken(null)` → `auth:session-expired` → `markFloorSessionExpired`) and the sign-in page then shows "This terminal was signed out." on a device that has **never** been signed in, re-armed every 2 minutes by `refetchInterval`. Codex's own workaround is the tell: `floor-bootstrap.spec.ts:84-95` gained a roster route stub this phase purely to keep the sign-in spec console-clean. **High (C3)** — the clock-in greeting renders `"morning, Marco"`: `dayPartGreeting()` returns the bare token `morning|afternoon|evening` and the controller interpolates it raw (`:383`), against the mapping table's "Good morning/afternoon, {first}" and `clock_in_result.png`. The unit test mocks the helper as `() => 'Good afternoon'` (`use-kiosk-flow.controller.test.tsx:26`) and asserts the mock's own output, and the Playwright case only asserts `toContainText('Marco')` — a false-green pair. **High (C4)** — `ClockKioskPage` returns `null` whenever a surface is open, so nothing is mounted beneath (measured: 0 `keypad-screen`, 0 `code-cell` while confirm is open), and neither surface page renders an opaque background, so both screens display through `RiseSurface`'s `bg-black/35` backdrop — the confirm screen is grey-dimmed and the result screen is doubly dimmed (confirm + result backdrops stack). Screenshots at 834×1194 against `clock_in_confirmation_of_user.png` show warm paper `#FAF8F4` replaced by grey. The Phase 3 device-settings rise page does this correctly by wrapping itself in `FloorKioskFrame`; the Phase 4 pages don't. **Medium:** (C5) surface content vanishes before the exit animation; (C6) every non-409 failure — including a failed post-action `/current` refetch after a server-side-successful clock-in — silently drops to the keypad with no feedback, inviting a double clock. **Low:** (C7) `roleLine`'s "first string value of the `role` record" fallback can print an id under the worker's name; (C8) a failed roster fetch makes every worker read as "No worker matches this code or email"; (C9) the action mutations keep the last worker's `user_id` after return (no `reset()`); (C10) the added `vite-env.d.ts` block duplicates the existing `ImportMetaEnv` and pushes `/// <reference types="vite-plugin-pwa/client" />` below a declaration, where TS ignores it; (C11) `dist/mockServiceWorker.js` ships in the production build as a `public/` asset (the mock *chunk* is correctly tree-shaken — the summary's claim is true only of the chunk); (C12) `KioskProvider` accepts and ignores `terminalLabel`/`workspaceName`; (C13) the barrel exports both surface page loaders hosts don't need, and `adapters={{}}` busts the provider's `useMemo` every render. **Notes:** (C14) 409 test fidelity; (C15) the tablet project was never run this phase. Phase 4 is **not** blessed; Phase 6 waits on C1/C2.
- 2026-07-30 Codex (Phase 4 corrections): completed and archived `PLAN_clock_kiosk_phase4_corrections_20260730`; C1–C15 are closed with C4 option (a), host-composed `FloorKioskFrame`, and no kit/`RiseSurface` change. Sign-in/settings typing and authenticated-only roster mounting are proven; greeting, opaque paper surfaces, always-mounted keypad, exit-content lifecycle, generic action retry, role/roster/mutation/config/build hygiene, real 409 fidelity, and tablet evidence all landed. Validation passed: root typecheck; kiosk 18/18; worker-shifts 36/36; UI 162/162; auth 3/3; API client 3/3; floor 8/8; combined kiosk/bootstrap Playwright 7/7 each on mobile, tablet, and desktop; floor lint/build; no production `mockServiceWorker.js`. Summary: `docs/architecture/implemented_summaries/SUMMARY_clock_kiosk_phase4_corrections_20260730.md`; archive record: `docs/architecture/archives/ARCHIVE_clock_kiosk_phase4_corrections_20260730_0730.md`. The C1/C2 Phase 6 implementation hold is released; master remains `approved` and unmoved.
- 2026-07-30 Codex (Phase 6 implementation): completed and archived `PLAN_clock_kiosk_phase6_clock_out_summary_20260729`. Added the pure marker-based analytics view-model, reused the existing stats insight-code map through `@beyo/stats/insight-copy`, preserved exact Phase 4 degradation for null/unusable analytics, and shipped exported scheduled-shift, announcements, items, week, and rate adapters with null/empty production defaults plus `VITE_FLOOR_MOCKS=1` showcase data. Clock-in scheduled/announcement sections and clock-out hero/insight/GAP gating are assembled without kit edits. Validation passed: root typecheck; kiosk 34/34; summary Playwright 2/2 each on mobile then desktop; full kiosk spec 7/7 each; floor lint/build; production showcase exclusion; diff check. The committed `AnnouncementsList` does not render the planned date, so the adapter retains ISO `date` for a later Claude-owned additive visual update. Summary: `docs/architecture/implemented_summaries/SUMMARY_clock_kiosk_phase6_clock_out_summary_20260729.md`; archive record: `docs/architecture/archives/ARCHIVE_clock_kiosk_phase6_clock_out_summary_20260729_0746.md`. Master remains `approved` and unmoved; Phase 7 is next.

- 2026-07-30 Opus (Phase 6 review): verdict **defects found** — 1 blocking, 3 high, 4 medium, 5 low, 2 notes. Corrections plan: `plans/PLAN_clock_kiosk_phase6_corrections_20260730.md` (C1–C6, C8–C11, C13 → Codex; C7, C12, C14 → Claude; **C7 needs an orchestrator decision**). **Checklist #1 (`analytics: null` degradation) — PASS.** The regression test exists and was run by the reviewer: `kiosk-summary: analytics null preserves the Phase 4 plain result` (`clock-kiosk.spec.ts:258`) green on `mobile`, `tablet` **and** `desktop`; it asserts `result-screen-out` + the stopped-task notice and the *absence* of `summary-screen`/`worked-today-plate`/`items-completed`/`week-bar-chart`/`rate-tile`, with exactly 2 `/current` and 1 clock-out request. Unit side: `toClockOutSummaryViewModel(null)` and both marker-incomplete variants return `null` (`analytics-view-model.test.ts:124`), the controller's plain-props path is asserted, and additive keys are ignored (`.passthrough()` plus an explicit `analytics.future_key` case). Two caveats, both filed: partial data degrades only if it *parses* (C8) and a bad `date` over-degrades (C4). **Checklist #2 (no fabricated data) — PASS, established by build output rather than by intent.** The gate is `import.meta.env.DEV && import.meta.env.VITE_FLOOR_MOCKS === '1'` (`FloorKioskProvider.tsx:15`); `DEV` is statically `false` in any build, so no env var can re-enable the fixtures in production. The reviewer built the floor app and grepped `dist`: "Line 3 changeover", "Hex bolt M8", "announcement-changeover", "Safety walk", "Payslips", "SCHEDULED", `showcaseKioskAdapters` and `KioskKitShowcase` are **0 occurrences each** (the one apparent hit was `M17 3h2a2` inside an SVG path, not `17.3`), and no `dist/mockServiceWorker.js` ships. Nothing is derived client-side: `analytics.timeline` is **never read anywhere** in `packages/clock-kiosk`, and no units/hour, week-hours or per-item computation exists — every GAP section comes solely from an adapter. Confirmed at runtime by re-running the summary specs with the flag off: hero + stopped-task notice + one insight row render, `items-completed`/`week-bar-chart`/`rate-tile` absent (screenshot captured at 834×1194). **Also verified clean:** kit integrity — `git diff c2c2ebc8 -- packages/clock-kiosk/src/components/` is **empty**, all 22 kit files byte-untouched since the kit commit, no additive prop needed, and no `Date`/`getTime`/`Intl` anywhere in the kit; marker math — IN/OUT/worked come from `started_shift`/`ended_shift` markers and never from bucket sums (proven by the test that inflates `pause_seconds` to 999,999 and still gets "8h 12m"), with the workspace zone applied via `formatTimeInTimeZone` (06:58Z/15:10Z → 08:58/17:10 for Europe/Stockholm) and design-matching formats ("8h 12m", mono HH:mm); single mapping module — `lib/analytics-view-model.ts` is pure and the components receive finished view models; phone ordering — the Playwright spec measures bounding boxes and asserts hours → insights → items → week below 640px and hours → items → week/rate → insights above it, green on all three projects; freshness caveat — insights are mapped exactly as delivered, with no client-side correction from timeline data; lifecycle bookkeeping — summary, `ARCHIVE_…_0746.md`, archived child plan, and a master diff of **exactly one** Review-log line. **Validation independently re-run and confirmed accurate:** `npm run typecheck` exit 0; `npm run test:clock-kiosk` 4 files / 34 tests; `npx playwright test --grep kiosk-summary` 2/2 on mobile, 2/2 on desktop **and 2/2 on tablet**; `--grep clock-kiosk` 7/7 on each of the three projects (21/21 total); floor `lint` and `build` green; `git diff --check` clean. **Blocking (C1)** — `vite.config.ts:79` moves the floor dev port to **5177** while `playwright.config.ts` still waits on `http://localhost:5175`, so as delivered every Playwright command in the phase's own validation plan dies with `Error: Timed out waiting 120000ms from config.webServer` — the reviewer hit exactly that and had to serve on 5175 explicitly to obtain the evidence above. `reuseExistingServer: true` is why Codex's run passed: a stale server was already listening. The change also contradicts master decision #1 ("dev port **5175**"), and neither `vite.config.ts` nor the `"dev": "vite --host"` switch (which publishes the kiosk dev server, unauthenticated `/sign-in` included, to the LAN) appears in the summary's Files-changed list. **High (C2)** — `analytics-view-model.ts:184-199` pairs the **first** `started_shift` with the **last** `ended_shift`, so a day holding two shifts overstates the hero: measured, 05:00–08:00 plus 14:00–17:00 (6h worked) renders **"12h 0m", IN 05:00, OUT 17:00**, silently counting the 6h off-clock gap. The handoff makes this reachable ("Both this app **and Connecteam** write the same shift machinery … a worker may already be clocked in"), and the test at `analytics-view-model.test.ts:33` locks the behaviour in by name. **High (C3)** — the subtitle date is `analytics.date`, which §5.1 defines as the clock-out's **UTC** date, rendered with a hardcoded `'en-GB'` locale and `timeZone: 'UTC'` (`:153-163, 203`), while the master mapping table prescribes "roster user + **client date**". Measured for Asia/Jerusalem with a 00:30-local clock-out: "Assembly · **Wednesday 29 July**" printed above "OUT **00:30**", with the header clock beside it reading Thursday 30 July — a screen that contradicts itself; `clock_out_result.png` shows the two dates agreeing. The captured production-default screenshot shows the same split. **High (C4)** — `:203-204` returns `null` for the *entire* view model when the date label fails to format, discarding a valid hero, valid IN/OUT and valid insights over a cosmetic subtitle: measured, `date: '2026-07-29T00:00:00Z'` and `date: ''` both drop a fully-valid analytics payload to the plain screen, and neither shape is excluded by the handoff — `BACKEND_REQUIREMENTS` #8 already records that §5.1 never annotates its formats. **Medium (C5)** — `BACKEND_REQUIREMENTS` #1 promises `scheduled_shift: {start, end}` *additive on `GET /worker-shifts/current`*, the response the kiosk already holds, but `ScheduledShiftAdapter`'s context is `{user, timeZone}` only (`types.ts:51-54`), so wiring the promised data later requires a public adapter-signature change rather than decision #11's "data change, not a UI change"; `AnnouncementsAdapter` is likewise synchronous against a promised *endpoint* (#2) with no async channel. **Medium (C6)** — one `ScheduledShiftDisplay {label, value}` is rendered verbatim by two surfaces the design labels differently ("Today's shift" on the confirm row per `clock_in_confirmation_of_user.png`, "SCHEDULED" on the plate per `clock_in_result.png`): `use-kiosk-flow.controller.ts:441` → `IdentityConfirmScreen.tsx:77` and `:488` → `DarkTimePlate.tsx:41`, and the shipped showcase adapter returns `label: 'SCHEDULED'`, so the confirm row reads "SCHEDULED 07:00 – 15:30". It also pushes time-zone formatting into host adapters, outside the single mapping module. **Medium (C7)** — the reused `@beyo/stats` map is manager-audience and evaluative, not the design readme's "factual statement + signed delta": "On a roll — 4-day streak", "Deep focus", "Choppy work", "Quality watch", "Leaving steps mid-shift", "Idle longer than usual" — all four spot-checked strings verified **present in the floor production bundle** — where the design's own examples are sentences ("142 units completed vs 5-day average of 130"). Three are negative performance judgements displayed to the worker on a shared terminal with the next person in line watching. Note the inversion: the *unknown-code* fallback ("Working time changed by +30m from the recent baseline") is more design-compliant than the mapped path. The plan mandated importing over forking and its own risk register names this exact drift, so it needs an orchestrator decision, not a unilateral fix. **Medium (C8)** — partial analytics fails the action instead of degrading: measured, omitting one `timeline` counter (or `timeline` itself) makes `ClockOutResultSchema` reject, so a **server-side-successful** clock-out surfaces as "Something went wrong. Please try again" back on the keypad, inviting the double clock-out that §5.1's hard rule exists to prevent. `AnalyticsTimelineSchema`'s five counters are required and `ClockOutAnalyticsSchema.timeline` carries no default (`worker-shifts/src/types.ts:66-77, 109`) — the same gap Phase 1's F3 correction closed for the sibling keys. The schema is Phase 1's and explicitly outside Phase 6's scope, so it is routed as a carry-forward rather than counted against this phase. **Low:** (C9) `toStatsInsight` (`:100-102`) returns `null` for any polarity outside `positive|negative`, so a known code arriving `neutral` silently loses its copy even though `insightPolarity` (`:82-87`) models `neutral`; (C10) `resolveKioskAdapters` merges `summaryExtras` by spread while the top level uses `??` (`kiosk-adapters.ts:29-32`), so a host passing `summaryExtras: { items: undefined }` — legal under `Partial<SummaryExtrasAdapter>` — hollows out the default and `gateSummaryExtras` throws; (C11) Playwright runs entirely under `VITE_FLOOR_MOCKS=1` (`.env.test:3`), so the adapter-default state that actually ships until backend phase 7 has no runtime guard — the reviewer verified it by hand, but nothing would fail CI if a GAP tile were accidentally enabled; (C12) `AnnouncementsList` drops the adapter's ISO `date`, leaving the design readme's "short, **dated** list" unmet against the component's own "dated" JSDoc (Codex declared this deviation and routed it to Claude — recorded so it is not lost); (C13) `SummaryScreen.tsx:113-118` keys insight rows on `insight.text`, which collides for two unknown codes sharing a metric and delta. **Notes:** (C14) at iPad portrait with production defaults roughly 550px of dead space sits between the single insight row and the bottom-pinned Done button — the pinned action is per design and Phase 4's plain screen behaves identically, so this belongs to Phase 7's design-fidelity pass; (C15) `WeekBarChart.tsx:18-22` formats durations inside a component against checklist #5's "no duration arithmetic in components", but the plan itself specified the seconds-in prop contract, so this is a kit-contract nit rather than a Codex deviation. Phase 6 is **not** blessed; Phase 7 should not start until C1 lands (its own validation plan cannot run otherwise) and C2/C3/C4 are fixed, since Phase 7's design-fidelity pass would otherwise sign off a wrong hero.

- 2026-07-30 Codex (Phase 6 corrections): completed and archived `PLAN_clock_kiosk_phase6_corrections_20260730`. C1–C11, C13, O1, O3, and criterion 16 are closed; C12/O2 were already present and C14 remains routed to Phase 7. Port 5175 was cold-start proven after terminating the stale listener. Marker pairing now uses the last IN before the final OUT, the subtitle date shares the client clock/workspace zone with the header, malformed analytics dates cannot discard the hero, partial timelines parse safely, scheduled-shift adapters receive fresh current state and surface-specific labels, worker insight copy uses the approved factual table with a neutral fallback while `@beyo/stats/src` remains unchanged, and nested adapters/stable keys/default GAP coverage are guarded. Authenticated idle preloads both kiosk chunks and kiosk Suspense now lives inside `FloorKioskFrame`. Validation passed: root typecheck; kiosk 52/52; worker-shifts 39/39; stats 142/142; UI 162/162; auth 3/3; API client 3/3; kiosk-summary 3/3 and full kiosk 8/8 on mobile/tablet/desktop; floor lint/build; production fixture exclusion; diff check. Summary: `docs/architecture/implemented_summaries/SUMMARY_clock_kiosk_phase6_corrections_20260730.md`; archive record: `docs/architecture/archives/ARCHIVE_clock_kiosk_phase6_corrections_20260730_1056.md`. Master remains `approved` and unmoved; Opus corrections re-review is the next gate.

- 2026-07-30 Opus (Phase 6 corrections re-review): verdict **pass with notes** — Phase 6 is **blessed**; every correction is closed against its measured baseline and no new defect was found. **C1 — CLOSED, cold-start proven by the reviewer**: `lsof -iTCP:5175 -sTCP:LISTEN` returned nothing, then `--grep kiosk-summary --project=mobile` booted the web server itself and passed 3/3; `vite.config.ts` serves 5175 again and `"dev"` is back to plain `vite` (no `--host`), both in the corrections summary's Files-changed list. **C2 — CLOSED**: `toClockOutSummaryViewModel` takes the final valid `ended_shift` and the last valid `started_shift` at/before it (`analytics-view-model.ts:202-234`); the two-disjoint-shift day (05:00–08:00 + 14:00–17:00) now renders **"3h 0m", IN 14:00, OUT 17:00** — the 6h gap is no longer counted (`pairs the final end with the last start on a two-shift day`), and the old defect-locking test is renamed/re-pointed to `uses the last start before the final end and tolerates truncation`. **C3 — CLOSED**: the subtitle formats `now` (the client clock) in the workspace zone (`formatClientDate`, `:175-187`; controller passes `new Date()`), with the cross-midnight case pinned — Asia/Jerusalem at 21:30Z renders "Thursday 30 July", agreeing with the header. **C4 — CLOSED**: `dateLabel` is nullable and only a genuinely unusable marker pair degrades to the plain screen; `date: '2026-07-29T00:00:00Z'`, `date: ''` and an unusable client label all keep "8h 12m" + IN/OUT. **C5 — CLOSED**: `ScheduledShiftAdapter` context gains `currentShift: CurrentShift | null`, `CurrentShiftSchema` gains additive `scheduled_shift` (round-tripped in `types.test.ts:86`), and the barrel documents announcements as a host-owned synchronous query seam. **C6 — CLOSED**: the adapter returns raw `{start, end}`; the controller formats once in the workspace zone and supplies "Today's shift" to the confirm row and "SCHEDULED" to the plate (controller test `:343,:376`; Playwright asserts both labels in the clock-in journey). **C7 — CLOSED per option (a) + amendment**: all eight authored sentences are wired verbatim in `KIOSK_INSIGHT_COPY` with per-metric semantics (counts int, rates one decimal, ratios ×100 one decimal, pauses as minutes — each pinned by a `renders factual kiosk copy for %s` case); unknown codes get the neutral metric+signed-delta line, **never** a stats title (there is no code path to one — only `KNOWN_INSIGHT_CODES` is imported); `git diff packages/stats/src` is **empty** (only `package.json` gained the `./insight-copy` subpath export) and `test:stats` 142/142. **C8 — CLOSED**: the five timeline counters and `timeline` itself default safely (`worker-shifts/src/types.ts:74-127`), with both omission shapes tested; 39/39. **C9/C10/C13 — CLOSED**: neutral polarity keeps authored copy; `resolveKioskAdapters` resolves `summaryExtras` per-key with `??` and survives explicit `items: undefined`; insight rows key on `insight.code` via the master-authorized additive optional `key` on `SummaryScreen` (no DOM/class change). **C11 — CLOSED**: `kiosk-summary: production adapter defaults keep GAP tiles absent` drives the real `adapters=undefined` branch via a `MODE==='test'`-only URL switch (unreachable in production — `import.meta.env.DEV` short-circuits first, verified in the built bundle) and pins hero/notice/insight present with items/week/rate at 0. **O1/O3 — CLOSED**: `preloadClockKioskSurfaces()` (new, exported) warms both package chunks from `FloorKioskProvider` once `user` is set; the host wrappers are synchronously warmed at registry-module scope so only package chunks can suspend, and they suspend into `KioskSurfaceSkeleton` (variant per surface) **inside** `FloorKioskFrame`; the MutationObserver proof in the clock-in journey saw neither `kiosk-surface-skeleton` nor the generic `surface-skeleton` after the idle beat, making the engine skeleton unreachable for both kiosk surface ids by construction and by observation. **C12/O2 — verified present** (kit diff `c2c2ebc8..99df0abf` is exactly `AnnouncementsList` + `KioskSurfaceSkeleton`); C14 stays deferred to Phase 7 as agreed. **Kit integrity**: working-tree `git diff -- packages/clock-kiosk/src/components/` touches exactly one file, `SummaryScreen.tsx` (+6/−2, the C13 prop). **Validation independently re-run**: root `typecheck` exit 0; kiosk 52/52; worker-shifts 39/39; stats 142/142; `kiosk-summary` 3/3 on mobile (cold start), tablet, desktop; `--grep clock-kiosk` 24/24 across the three projects; floor `lint` and `build` green; `dist` grep — showcase fixtures and `showcaseKioskAdapters` 0 occurrences. **Notes (no action required this phase):** (N1) importing `KNOWN_INSIGHT_CODES` bundles the whole `INSIGHT_COPY` map, so the manager title templates ("On a roll — …-day streak" etc.) still *ship* in the production `KioskProvider` chunk as dead data — unreachable by any render path, so C7's screen criterion holds, but a future standalone codes module in `@beyo/stats` would drop the dead strings; Phase 7 may pick this up. (N2) the O3 proof is negative-form (no skeleton observed post-preload) plus warm-by-construction; a throttled cold-load assertion that `KioskSurfaceSkeleton` *does* appear inside the frame would strengthen it but criterion 15's stated proof is exactly the negative form. **Phase 7 may start.**

- 2026-07-30 Codex: **capability complete.** Phase 7 is archived with
  `SUMMARY_clock_kiosk_phase7_validation_polish_20260729.md` and
  `ARCHIVE_clock_kiosk_phase7_validation_polish_20260730_1015.md`. Codex
  validation passed: root typecheck; stats 143/143; worker shifts 40/40; kiosk
  54/54; floor 9/9; and `clock-kiosk` Playwright 9/9 on mobile, tablet, and
  desktop. Claude's criterion-7 fidelity/a11y pass is recorded in the archived
  child plan, including its post-fix 27/27 three-project evidence. All v1
  kiosk backend routes remain mocked because the handoff liveness table marks
  them ❌; the per-endpoint future flip procedure is in
  `packages/clock-kiosk/README.md`.

- 2026-07-30 Opus (Phase 7 review — **capability close-out**): verdict **complete-with-notes** — **master acceptance criteria 1–9 all PASS**; no blocking defect; 2 medium and 6 low findings. Corrections plan: `plans/PLAN_clock_kiosk_phase7_corrections_20260730.md` (F1 → Codex, +Claude only if option (b); F2–F5, F7 → Codex; F6 → operator; F8 → orchestrator decision). Nothing in it blocks the capability's `completed` state. **Validation independently re-run by the reviewer, and every number in the summary is accurate:** `npm run typecheck` exit 0 (five app workspaces + the full package chain, `worker-shifts` and `clock-kiosk` both explicitly in it); `test:worker-shifts` 5 files / 40 tests; `test:clock-kiosk` 4 files / 54 tests; floor `test:unit` 5 files / 9 tests; `test:stats` 17 files / 143 tests; `test:auth` 2 files / 3 tests; `test:api-client` 1 file / 3 tests; `test:ui` 29 files / 162 tests; `--grep clock-kiosk` **9/9 on mobile, 9/9 on tablet, 9/9 on desktop** — and these were obtained **cold**: `lsof` showed a leftover plain `vite` dev server on 5175, which was terminated first so the Playwright config booted its own `--mode test` server (see F7). **Criteria 1–9, each dispositioned.** (1) *Contract fidelity* — **PASS**: `worker-shifts` wraps exactly the six handoff routes, its MSW handlers cover all of them (`mocks/handlers.ts:75,81,90,117,153,217`) including the declared-states pair kept wrapped-but-unconsumed per decision #10, and a repo-wide grep for the legacy `POST /worker-shifts/clock` returns **zero** hits outside `node_modules`. (2) *Dependency arrows* — **PASS**: the floor app's every `@beyo/*` specifier is a barrel or a *declared* subpath (`@beyo/worker-shifts/mocks`, `@beyo/clock-kiosk/showcase`) — zero deep imports; `worker-shifts` has **0 `.tsx` files** and imports only `@tanstack/react-query`, `@beyo/lib`, `@beyo/api-client`, `zod`, `msw`, `react` — zero UI; `clock-kiosk` imports only `react`, `@beyo/lib`, `@beyo/worker-shifts(/mocks)`, `@beyo/ui`, `zustand`, `@beyo/stats/insight-codes` and (test-only) `@beyo/api-client` — zero app code. (3) *Mocks-offline end-to-end + one-flag live switch* — **PASS**: 27/27 Playwright fully mocked with the backend absent, and `VITE_FLOOR_MOCKS` is genuinely the single flag — it gates the MSW worker (`main.tsx:18`) and the showcase adapters (`FloorKioskProvider.tsx:24`), with `.env`=1 / `.env.test`=1 / `.env.production`=0, so flipping to live is an env change with no code change. (4) *The four handoff UX rules* — **PASS**: fresh `/current` before every action render, 409-as-refresh (verified against a real `ApiRequestError` in Phase 4 and re-pinned in Phase 6), `analytics: null` → plain success screen, universal auto-return, one generic no-match message, and no identify endpoint anywhere — the pinning tests were all re-run green; the one blemish is that the *offline* state now borrows the miss signal (F1). (5) *Floor auth + three-app invariance* — **PASS**: `test:auth`, `test:api-client` and `test:ui` all green, on top of Phase 2's and Phase 3's branch-by-branch proof that zero behavioral diff is reachable under `manager`/`worker`/`seller`, with M1 closed by the floor-gated `finally`. (6) *Design fidelity + ≥44px + mono numerals* — **PASS as recorded**: Claude's criterion-7 entry is concrete, not "looks good" — three-project captures against all five images, named fixes f1/f2, *measured* contrast (ink-on-key 14.3, white-on-accent 6.6, white-on-success 6.4, plate-label 5.1, error 4.7), and 27/27 post-fix; `--color-kiosk-secondary` 3.53:1 / `--color-kiosk-tertiary` 2.62:1 are correctly disclosed as large/decorative-only with the remedy recorded. (7) *Every GAP behind an adapter, each documented* — **PASS**: `DEFAULT_ADAPTERS` returns `null`/`[]`/`null` for scheduled-shift, announcements, items, week and rate, `gateSummaryExtras` nulls empty collections, and `BACKEND_REQUIREMENTS` §1–§7 covers all seven GAP rows. Reading note: the badge/station GAP (§6) has **no** adapter — the mapping table itself resolves it as "role name only", so it degrades by omission by design, not by oversight. (8) *Phase boundaries* — **PASS**: the two historic bleeds (`.kiosk-shake` landing in Phase 3, analytics parsed-unrendered in Phase 4) were both dispositioned in their own reviews. (9) *Master implements no code, edits neither design nor handoff* — **PASS**: the master holds no code, and the five images, `design_readme.md` and the handoff are unmodified. **Also verified clean:** the Review log is complete across every executed phase (1, 1-corr, 1-re-review, 2, 2-review, 3, 3-M1, 3-review, 3-corr, 4, 4-review, 4-corr, 6, 6-review, 6-corr, 6-re-review, 7); Phase 5 is coherently `set_aside`; the phase-7 summary, `ARCHIVE_…_1015.md` and the archived child plan all exist; resilience is really covered, not nominally — `resyncs immediately when the terminal regains focus`, `refetches the fresh roster when the kiosk regains focus`, `returns to the keypad when a hidden confirmation outlasts inactivity`, `continues matching from stale roster data after a roster error` and `disables matching without showing a no-match error when no roster is available` all exist and all pass; no `packages/worker-shifts/README.md` is *not* a gap — no package in the repo except `clock-kiosk` has one, and only the kiosk README was ever required. **Medium (F1)** — the always-on idle screen raises the *code-miss* signal for a *roster outage*. `use-kiosk-flow.controller.ts:462` computes `error: flow.error || rosterQuery.isError && !hasRoster`, and `CodeCells.tsx:22-27` fires `.kiosk-shake` on exactly that false→true transition while `:44-48` paints all four cells `border-kiosk-error/60` for the entire outage. So a terminal that loses its roster shakes its empty keypad and holds four red cells at a worker who has typed nothing — the design's wrong-code language, used for a network fault. The phase plan promised a "quiet 'terminal offline' state" and the Review log claims one; the test at `use-kiosk-flow.controller.test.tsx:465` is named `disables matching without showing a no-match error when no roster is available` yet **asserts `error).toBe(true)`**, locking the defect in by name in the same way Phase 6's C2 did. **Medium (F2)** — the integration README's registration snippet is not copy-safe, which defeats the criterion-6 purpose ("could another app mount the kiosk from the README alone?"). `README.md:120-122` correctly *tells* a host to wrap each lazy surface in host chrome and a `Suspense` fallback, but the snippet at `:137-148` builds bare surfaces via `Object.fromEntries` and never uses the `KioskSurfaceSkeleton` it imports at `:129`. A host copying it gets kiosk pages with no opaque paper background, rendering through `RiseSurface`'s `bg-black/35` — **exactly Phase 4's blocking C4, re-shipped as documentation**. The reference shape a host actually needs is `apps/floor-app/.../src/app/surface-registry.ts:27-58`. **Low:** (F3) criterion 4 asked that `index.ts` "export exactly the documented surface" and it does not, in both directions — the README's Phase 6 table (`:34-45`) documents eight components of which **0 of 8** are exported (grep-verified), and its "Barrel note" (`:47-50`) still promises "The Phase 6 Codex session adds these exports when it wires the screen" although Phase 6 ran and correctly kept them internal, while `preloadClockKioskSurfaces` — a host-facing export the floor app depends on at `FloorKioskProvider.tsx:56` — and `KioskSurfaceSkeleton`'s `variant` values are documented nowhere; (F4) the README's `@source` list (`:74-81`) omits `@beyo/lib`, which the reference host does carry (`index.css:6`) and which Phase 3's review recorded as required by `14_styling.md` §14, and the `@font-face` block shows one IBM Plex Mono weight where the host registers three (`index.css:18,26,34`); (F5) `packages/clock-kiosk/package.json` declares `@beyo/auth`, `@beyo/hooks`, `@tanstack/react-query` and `zod` as peers that **no** file under `src/` imports, and omits `@beyo/api-client`, `vitest` and `@testing-library/react`, which it does — `@beyo/api-client` only from a test file, so it belongs in `devDependencies`; this is the class Phase 3's C6 fixed for the floor app and CF1 fixed for `worker-shifts`/`msw` in this very phase, so the public-API audit stopped one package short; (F6) the manual rehearsal is **scripted but never executed** — Codex's own Review-log entry made it a closure gate ("Claude's design-fidelity/a11y entry **and target-device manual rehearsal** are required before the summary/archive/master closure transition"), and closure proceeded on one of the two, leaving physical sleep/wake clock resync and true network-loss behavior unverified; it needs a device, so it is routed to the operator to run or to waive explicitly; (F7) `playwright.config.ts:32-37` keeps `reuseExistingServer: true` with no check that the listener is the `--mode test` server — during this review a leftover plain `vite` dev server (PID 68570, no `--mode test`) held 5175 and the suite would have silently run every spec without `.env.test` loaded; this is the **unfixed root** of Phase 6's blocking C1 (that correction moved the port back but left the reuse semantics) and of the "two-process port-5175 collision briefly poisoned runs" Claude's own fidelity entry reports; (F8) the master is `status: completed` but still lives in `under_construction/`, and no decision records why — staying is defensible while the folder holds the `set_aside` Phase 5 plan and the live `BACKEND_REQUIREMENTS` gap spec, but that should be a recorded call, not a default. **On Claude's declared seam crossing (f2):** accepted. The 3-line ISO→"29 Jul" formatter in `lib/kiosk-adapters.ts` is a logic-file edit by the visual owner, correctly self-declared for objection; it is production-inert (the default announcements adapter returns `[]`), covered by the green suites, and lives in a `lib/` mapping module rather than a component — the right home for it. One note rather than a finding: it hardcodes `'en-GB'` and `timeZone: 'UTC'`, the pattern Phase 6's C3 removed from the summary subtitle; UTC is correct for a date-only value, but the locale should follow the workspace convention if announcements ever ship. **The capability is complete.** Criteria 1–9 hold, the mock/live boundary is honest — every v1 route still ❌ in the handoff, every handler retained, pause-reasons untouched with the shelved declare flow — and the flip runbook is in the kiosk README. F1 and F2 are worth fixing before another app mounts the kiosk: one shows a worker a false error on the screen the terminal rests on, the other would hand the next host a defect this capability already paid to find.

## Lifecycle transition

- Current state: `completed` (2026-07-30)
- Completed sequence: 1 → 2 → 3 → 4 → 6 → 7
- Transition owner: Codex lifecycle closure after recorded Claude criterion-7 pass
