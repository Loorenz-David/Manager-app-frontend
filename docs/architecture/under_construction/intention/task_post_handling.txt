INTENTION:

We will now begin making the abstraction of some of the task foundations of the manager application into packages.

This movement has to be done correctly, previously we have done this by creating the files on packages then wiring them into another app and when they work we make the transtion to the manager application so that it can consume them instead of the locals,
but this technique has failed with the agents as the agents simplify the UI and functionality of the components it makes on packages.

So what we will do now is to make the full movement of the files to the packages, make the necessary adjustements for them to work as packages and make the manager appliction consume them directly.

we will start by moving the TasksPage which uses the  TasksView to the task packages.

this will require moving the provider and all the logic behind it to the packages also, the goal is to make the manager application consume this task page component fully from packages.

GOAL:
make an implementation plan for codex using the template @template_plan . use the correct contracts for this implementation using the contract guide @frontend


RELEVANT FILES:

manager task components:

/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/frontend/apps/managers-app/ManagerBeyo-app-managers/src/features/tasks/components/TasksView.tsx

/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/frontend/apps/managers-app/ManagerBeyo-app-managers/src/pages/tasks/TasksPage.tsx

/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/frontend/apps/managers-app/ManagerBeyo-app-managers/src/features/tasks/providers/TasksViewProvider.tsx