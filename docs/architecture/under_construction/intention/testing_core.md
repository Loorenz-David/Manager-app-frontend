we will add a feature called test_feature
in it it will have the folder architecture specify by 01_architecture folder structure.
we will create a couple of test pages with test content and buttons to interacts with the sliding pages
we will subscribe one page to the bootom slide-up
another page to the full page slide

another page which will render at the app root with two buttons to call the bottom up page and slide page to see how they interact. with in the those two pages inside those surfaces we will add the buttons to call their cousing surfaces so that we can test how they interact nesting pages. we should add close buttons at the top so that i can close them.
