


At the frontend on the task package feature, we will create one new field that will be used as part of a form. this field will follow exactly the same principles as the item identity field, but the store key values are item_position and item_zone the slide box labels are Zone and Wagon ( wagon for item positioned ) .  both are strings values .
and the parent can control which input field should render on load. 

/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/frontend/packages/items/src/components/ItemIdentityField.tsx

The input for Zone ( item_zone ), will perform a request query call to the endpoint ###/items/location ( documented at HANDOFF_TO_FRONTEND_item_zone_batch_position_update_and_location_search_20260707 )  , sending the parent injection for article_number or sku as "q" param , it prioritizes the sku over article_number, article_number is a fallback . 

/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/frontend/docs/handoff/from_backend/HANDOFF_TO_FRONTEND_item_zone_batch_position_update_and_location_search_20260707.md


the response will only be of value when the match is precise on requested identity, if more than one record comes it takes in the first on in the list, and fills up that position label on the zone field.

The forms should now inject the filled up item identity to this field for this process to work,  this is a stand alone field component self sustainable thus the query flow is own by the field component. 

the current forms: InternalFormContent, PreOrderFormContent, ReturnFormContent will replace their current position field with this new field component, and will now supply the item identity input through the correct params of the component. 

when the form is ReturnFormContent  the position field will open on Zone field .  

We should update all the schemas that has item_position so that they will now also have the item_zone key value. 

Once this is implemented we will then implement the visual preview of this zone and position on the TaskDetailPages of the TaskDetailPage in packages and on the workers page. 


package page:
/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/frontend/packages/tasks/src/pages/TaskDetailSlidePage.tsx

workers page:
/Users/davidloorenz/Desktop/Developer/BeyoApps_2025/ManagerBeyo-app/frontend/apps/workers-app/ManagerBeyo-app-workers/src/pages/task_steps/TaskDetailSlidePage.tsx

This pages currently render only the item_position with a label on the left side that labels it as Wagon with an icon. This will change.
We will now display the item_zone and the item_position in the following way:  item_zone  |  #{item_position} . Separate elements with a divisor line. the user can tap either of them and this will display the bottom sheet page with the position/zone field we just build . depending on the tap it will open with the slide box selected on the item_zone or in the item_position. 
Opening in this page should not generate the fetch query to the external api ( so we will need a flag to pass to the component to prevent the fetch ). saving the modifications sends the request to update the item . that router for patching the item position can now take the payload key for item_zone .

This preview labels don't render any icons, the zone preview label is capitalize on letters. 


GOAL:
make an implementation plan for codex using the template @template_plan . use the correct contracts for this implementation using the contract guide @frontend




