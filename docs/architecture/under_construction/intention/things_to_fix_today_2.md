✅
on pre-order and return tast from, designer field should be placed at the end of the container it lives.

✅
we need to create upholsteries on bootstrap, with images.

we must add images to the working sections also

on form loads the fields placed on the form should already load their primitivs and pages.

where is the use session object being stored? i don't see it in local storage, and when page reloads i get : equest URL
https://api-manager.beyoworkaroundtheclock.com/api/v1/auth/refresh
Request Method
POST
Status Code
403 Forbidden response: {"error":"Refresh token missing.","ok":false} . even on published app with https.

can we make it pwa already?
