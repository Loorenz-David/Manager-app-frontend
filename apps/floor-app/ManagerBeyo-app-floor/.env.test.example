# Copy to `.env.test` to run the Playwright/Vitest suites. Never commit the copy.

# Empty so the test build issues same-origin requests, which MSW intercepts.
VITE_API_URL=
API_TARGET_URL=

# "1" routes every request through the MSW mock backend.
VITE_FLOOR_MOCKS=1
