# Vite dev-server proxy target (server-side only, never in the browser bundle).
API_TARGET_URL=http://192.168.1.246:8000

# Empty in dev so the browser uses same-origin /api/... requests through Vite.
VITE_API_URL=http://192.168.1.246:8000
VITE_WS_URL=http://192.168.1.246:8000
PLAYWRIGHT_TEST_EMAIL=admin@beyo.dev
PLAYWRIGHT_TEST_PASSWORD=Admin1234!
